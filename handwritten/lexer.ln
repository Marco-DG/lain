type Lexer {
    text *u8[]
    pos  usize in text
}

func lexer_new(text *u8[]) Lexer {
    return Lexer(text, 0)
}

proc main() i32 {
    src u8[8] = "x + 42 ;"   // 8 chars, remaining bytes are null (sentinel)
    var l = lexer_new(src)

}