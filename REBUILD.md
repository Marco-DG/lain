# Lain Compiler — Clean-Core Rebuild Plan & Tracklist

> **North star.** Lain's identity is *deterministic static proof for safety and zero-cost*.
> Its proof engine therefore has to be world-class — clean, principled, powerful,
> complete. Today the middle-end is a sound-but-accreted AST-directed, name-keyed,
> single-pass mess with ~70 hand-coded recognizers and no CFG. We rebuild the **core**
> (not the language, not the frontend) around a typed SSA/CFG IR and a real relational
> abstract interpreter (octagons + fixpoint), **in place, beside the old engine**,
> brought up by **differential testing against the existing corpus**, phased so the
> corpus is green at every checkpoint.

This file is the single source of truth for the effort. **Update the Status log at the
bottom every session.** Check boxes as work lands. Keep it honest.

---

## Ground rules (non-negotiable)

- **The corpus is the gate.** ~610 tests + trust harness + 14 execute-accepted fuzzers.
  Never regress it. It is green at the end of every phase. It is *the* reason a clean
  rebuild is survivable here.
- **Coexistence, not big-bang.** The new engine is built *alongside* `src/sema/`. A
  build flag selects old vs. new. The old engine is the reference oracle during bring-up.
- **Differential bring-up.** Every analysis change: run old vs. new on the whole corpus +
  fuzzers, diff accept/reject. `new rejects what old accepted` ⇒ precision regression to
  fix. `new accepts what old rejected` ⇒ soundness check (execute under ASan/UBSan).
- **Each phase ships a working compiler.** Never more than one phase from green.
- **Soundness first, precision second.** Every analysis change is fuzzed (execute-accepted).
- **Keep the frontend.** Lexer, parser, AST work — reuse them. We are not re-litigating
  parsing or redesigning the language (analysis18 closed; the spec exists).
- **Docs track reality.** Design docs are written from the actual implementation and
  updated as we learn; the spec carries the *contract*, an annex carries the *design*.

---

## Target architecture

```
source
  │  (KEEP) lexer → parser → AST
  ▼
AST
  │  (KEEP, then port) name resolution + type checking → typed AST
  ▼
typed AST
  │  (NEW) lower  →  typed SSA/CFG IR          src/ir/
  ▼
IR (functions = CFG of basic blocks; SSA values with identity)
  │  (NEW) analysis passes over the IR         src/analysis/
  │        • VRA = octagon abstract interpreter (fixpoint + widening)
  │        • borrow / region
  │        • linearity / ownership
  │        • effects, termination
  ▼
checked IR
  │  (NEW) backend                              src/ir/emit_c.h  (later: LLVM seam)
  ▼
C  (or LLVM IR later)
```

## ★ THE REFRAME — IR SOVEREIGNTY (adopted 2026-08-28, user-directed)

A major clarification of the endgoal, and arguably the most important decision in the
whole rebuild. **The Lain-IR is the semantic and soundness AUTHORITY of the system.** It
does not depend on the front-end; the front-end (present or reworked) and the backend
(emit-C now, LLVM/native later) are *replaceable clients* of it.

> The IR depends on nothing. Everything — every front-end, every backend, every analysis —
> depends on the IR. A program's meaning (its concretization γ) is definable from the IR
> ALONE, with the AST already freed.

**What Lain-IR IS: a full VERIFICATION IR that is also the codegen substrate.** One IR,
simultaneously:
- **semantic authority** — total, self-contained, precise meaning (no "incomplete" escape
  hatch; unknowns are *modeled* as `opaque`/havoc with a declared effect footprint, so the
  IR is always total AND sound);
- **verification substrate** — VRA (numeric: bounds/overflow/div/termination), borrow/region
  (memory safety), linearity/ownership (resource safety), effects (purity/termination/errors)
  are ALL IR passes reading IR facts, unified by an **`assume` / `assert` / `requires` /
  `ensures`** layer + a rich type lattice (refinements, regions, linearity, effects). Each
  safety analysis is a *consumer* discharging one class of assertions;
- **codegen substrate** — lowers to C now, LLVM/native later.

**Precedent (why this is ambitious but not unprecedented).** LLVM IR is the *opposite* of
this: it exploits UB (`undef`/`poison`) as an optimization license — its ecosystem grew
Alive2/Vellvm to cope. The nearest precedents are **Rust MIR** (borrow-checks on the
mid-level IR — closest single analogue), **CompCert** (machine-checked IR semantics), and the
verification-IR family **Boogie / Why3 / Viper** (+ refinement types: LiquidHaskell / Flux /
RefinedC). The novel part is the FUSION — those traditions are normally separate (MIR isn't
refinement-level; Viper isn't the codegen path; LLVM is codegen but UB-exploiting). Lain-IR
is one IR that is *both*.

**Two axes of the goal (keep distinct):**
1. **Coherence / independence** (engineering): the IR carries its own types, layout, symbols,
   refinements, effects; imports no front-end header. *Litmus test:* `src/ir/*` and
   `src/analysis/*` compile against `ir.h` alone (no `ast.h` / `token.h`). This is where
   "soundness independent of the front-end" is won — the IR becomes the soundness authority,
   catching unsoundness even when a (reworked) front-end emits it.
2. **Semantic + verification ceiling** (power): the IR spans the full domain — Turing control,
   a refinement/interval type lattice, an effect lattice, an explicit memory/aliasing/region
   model, first-class assume/assert/contracts. The octagon is *one* domain over it; polyhedra,
   congruences, separation logic can be added without touching the IR. The "intricate
   specializations" (SIMD, dependent lengths, `a[n-1-i]`) are mostly **holes in the IR's
   primitive set**, not real specializations — add the primitive and they become ordinary.

**Sequencing consequence (reorders the plan):** do the sovereignty work (Move A + B below)
*BEFORE* porting the remaining analyses (old Phase 3). Porting borrow/linearity while the IR
still leaks to the AST would make every ported pass inherit the coupling. Fix the substrate
first. See **Phase 2.9** for the tracklist.

*Caveat we accept:* "independent" ≠ "ignores the front-end" — the IR still DEFINES the
contract the front-end must meet (the builder API is that interface); the dependency points
inward to the IR.

### Key design decisions (finalize in Phase 0, record in the docs)
- **Numeric domain: octagons** (`±x ± y ≤ c`) — a superset of difference-bounds (`x−y ≤ c`),
  the relational power that cracks the deferred frontier (merge `o=i+j`, Lomuto `i≤j`,
  symbolic 2D `i*w+j<h*w`). O(n²) space / O(n³) closure **per variable pack** ⇒ **variable
  packing is mandatory** for scalability. *Staging option:* land a clean, fully-*closed*
  difference-bound domain first (covers most bounds idioms), then generalize to octagons.
- **Analyze over mathematical integers (ℤ).** Overflow is a *separate* proof obligation
  (Lain proves no-wrap anyway), so the numeric domain assumes no wrap and the overflow
  checker guards it. Clean and standard.
- **Fixpoint:** worklist over the CFG; widening at loop headers after N iterations; optional
  narrowing pass. Loop invariants (incl. today's bounded-counter) are *discovered*, not
  pattern-matched.
- **IR is a typed CFG in SSA form.** SSA gives value identity (kills name-keying) and clean
  φ-at-join. *Staging option:* CFG + domain-join at merge points first; full SSA construction
  can follow — the essential thing is CFG + join + fixpoint.
- **Spec split:** normative *contract* in `spec/` chapter 13 (what the VRA guarantees, not
  how); *design* in a spec annex (the octagon domain + algorithm) — first-class & permanent.

---

## Phase 0 — Blueprint (the design, where we commit)  ⟶ deliverable: docs

- [x] **0.1 Compiler-architecture doc.** → `design/architecture.md`. Target pipeline,
      module layout (`src/ir/`, `src/analysis/`), old↔new coexistence + `--engine` selector +
      `diff_engines.sh` differential strategy, keep/rebuild/retire boundaries, non-goals.
- [x] **0.2 IR spec.** → `design/ir.md`. Types, SSA values + memory model (alloca/load/store
      → mem2reg), instruction set, terminators (`br_cond` = the guard), CFG/functions/module,
      AST→IR lowering per construct (if→φ-join, while→back-edge header, match→switch,
      break/continue→edges), IR→C backend, worked example. The keystone.
- [x] **0.3 VRA / octagon design.** → `design/vra-octagon.md` (→ spec annex at 0.5). Octagon
      domain (DBM over ±dims), closure/join/meet/widening/projection; the γ-invariant as the
      soundness yardstick; transfer functions per IR instr; CFG fixpoint + widening at headers;
      consumers (bounds/overflow/div/refinement); variable packing (mandatory); ℤ+overflow
      split; the nonlinear escape-hatch (catalogued lemmas, not recognizers); §8 maps the ~70
      current recognizers to (a) falls-out / (b) small transfer case / (c) nonlinear lemma.
- [x] **0.4 Recognizer catalog.** → `design/recognizer-catalog.md`. All ~60 special-cases in
      `ranges.h`/`bounds.h`/`sema.h` enumerated + classified. **Result: ~50 (a) fall out of the
      octagon+CFG fixpoint (incl. the entire `__len_`/name-key machinery and ALL loop-invariant
      recovery — liv/affine/bounded-counter/clamp-join/post-loop), ~9 (b) small nonlinear
      transfer cases (mask/div/mod/shift/movemask/popcount), 1 (c) nonlinear lemma
      (flattened-2D).** The pile is proven redundant, then deleted. This is the Phase-2.8 gate.
- [x] **0.5 Normative spec contract.** `spec/chapters/13-vra.tex` Overview rewritten to the
      abstract *contract*: normative Soundness (over-approximation ⇒ accepted-is-safe;
      prove-or-reject) + Determinism/decidability (poly-time, no SMT) constraints, explicitly
      decoupled from the domain/algorithm, pointing to the design. Spec recompiles clean.
      *(Full VRA design ANNEX deferred to end of Phase 2 — written from the real engine, per
      "docs track reality"; `design/vra-octagon.md` is the design home until then.)*
- [x] **0.6 Review + lock.** Docs cross-consistent (architecture→ir→vra-octagon→catalog).
      **Domain decision locked (see log):** stage a fully-*closed difference-bound* domain
      first (covers ~all current recognizers), then add the octagon `+`-family for the
      relational frontier (merge/interleave `i+j<len`); ℤ + separate overflow obligation;
      variable packing mandatory; CFG + join first, SSA/mem2reg as a follow-up.

## Phase 1 — IR foundation (the keystone)  ⟶ deliverable: faithful IR pipeline

- [x] **1.1 IR data structures + builders** (`src/ir/ir.h`, `src/ir/build.h`): IrType,
      IrValue (SSA id = identity), IrInstr (slice_len/elem_ptr/wrap-mode/aux), IrTerm, IrBlock
      (φ + body + preds + loop-header flag), IrFunc/IrModule; builder + convenience-emitter
      definitions; `ir_finalize_cfg` (predecessors + back-edge loop-header detection).
      **Validated** by `src/ir/test_ir.c` (hand-built `maxi` branch + `count` loop → correct
      SSA-form dump; loop header auto-detected). Not wired into main.c; old engine green.
- [~] **1.2 AST → IR lowering** (`src/ir/lower.h`): **core subset WORKS end-to-end on real
      programs.** Type bridge (AST Type→IrType), expressions (literal/ident/binary/unary/
      call/index/member.len/cast), statements (var/assign/if-else/while/return/expr/break/
      continue/unsafe) → memory-form IR (alloca/load/store); `src/ir/lower_driver.c` reuses
      the frontend (load_module+sema_resolve_module→typed AST→lower→dump) and lowers
      `tests/ir/sum_maxi.ln` to a correct CFG (loop header auto-detected; ℤ-widened `i33`
      add results — the overflow-split model). TODO: for/match/structs/strings/short-circuit
      and-or/slices-fully → grow toward the 1.6 gate.
- [~] **1.3 IR → C backend** (`src/ir/emit_c.h`): **scalar core round-trips END-TO-END.**
      Blocks→labels+gotos; SSA values→C locals declared at function top; alloca/load/store→
      real slot pointers (C compiler optimizes away); int/bool + arithmetic/cmp/call/cast/
      branch. `lower_driver.c --emit-c` emits a module; `tests/ir/sum_maxi.ln` lowered →
      emitted C → gcc → **runs correctly (sum(5)==10, exit 0)** through the whole NEW pipeline.
      Arrays/slices/structs/strings (fat-pointer repr) grow next.
- [ ] **1.4 Pipeline wiring:** `--engine=ir` (or `LAIN_IR=1`) selects the new path end-to-end.
- [~] **1.5 IR dumper** (`src/ir/dump.h`): readable SSA-form printer done + validated on
      hand-built IR. Round-trip on real *parsed* programs comes with lowering (1.2).
- [ ] **1.6 GATE:** every `*_pass` test compiles through the new pipeline and the emitted C
      runs identically (trust + fuzzer executables pass). `*_fail` rejection is deferred to
      Phase 2 (no analysis yet). Corpus faithfulness proven.

## Phase 2 — Octagon VRA (the payoff)  ⟶ deliverable: analysis that dominates the old

- [x] **2.1 Octagon domain** (`src/analysis/octagon.h`): DBM over 2n ± dimensions (Miné);
      coherent setters, Floyd–Warshall + strong (integer-tight) closure, ⊥ test, join (⊔ max),
      meet (⊓ min), widening, order, forget/projection, interval read. **Brute-force validated**
      (`test_octagon.c`): closure preserves γ, ⊥ sound, join/meet/widen over-approximate, meet
      exact, widening converges — 40000 random trials over ℤ³ + hand-built tightness cases.
- [ ] **2.2 Variable packing** strategy (cluster vars that co-occur in constraints/indexing).
- [~] **2.3 Fixpoint engine** over the CFG (`src/analysis/vra.h`): sweep-to-fixpoint, join at
      merges, **widening at loop headers** (back-edge detected in `ir_finalize_cfg`). Worklist
      ordering + narrowing pass = polish TODO.
- [~] **2.4 Transfer functions** — const, load/store (memory-cell model: an alloca id doubles
      as its scalar cell, so `i=i+1` becomes a difference fact across iterations), add/sub with a
      constant operand, slice_len (≥0, canonicalized per slice), cast-as-copy, **branch/guard
      refinement** (`i<n` ⇒ the octagon fact on the taken edge). TODO: φ (once mem2reg lands),
      two-variable arith, calls/return-refinements.
- [~] **2.5 Consumers:** **bounds** (`0 ≤ idx < len`, fixed arrays *and* slices), **overflow**
      (CHECK-mode +,−,× — ℤ result range vs the operand type, §2.6), **div-by-zero** (divisor
      range excludes 0), and **termination** (a structured loop with exactly one well-formed step
      `cell = load(cell) ± c` draining a loop-invariant bound) — all four done and validated:
      `test_vra.c` **18/18**, and each fuzzed sound (`fuzz_vra.c`: bounds 200k, overflow-range
      300k, termination 100k trials, 0 unsound). The relational + nonlinear reach (sliding-window,
      two-pointer, reverse, mask/modulo/shift) is here too. Known precision gap (sound): usize/u64
      overflow at 2^64 (int64 domain). TODO: refinement types; u64-aware overflow.
- [~] **2.6 Differential harness** (`vra_survey.sh`): the ⊇-accepts direction is measured — over
      every old-accepted indexing program, the new octagon VRA proves **411/510 bounds
      obligations (81%)**, 40 programs fully. Remaining gaps are lowering-side (nested
      array-in-struct, global arrays, `in`-loops) or off-North-Star (SIMD). Full reject-side
      differential (needs the new engine wired as an alternative pass, not gated by sema's exit)
      is TODO.
- [ ] **2.7 Recognizer parity:** walk the 0.4 catalog; confirm each falls out or add the
      extension. Fuzz each addition (execute-accepted).
- [ ] **2.8 GATE:** new engine passes the ENTIRE suite (pass + fail) and all fuzzers, and
      *dominates* the old (⊇ accepts, ⊇ sound rejects). Then it can become default for VRA.

## ★ Phase 2.9 — IR SOVEREIGNTY (the keystone; do this BEFORE Phase 3)

Make the IR the total, self-contained, verification-native authority. Two moves; the ORDER
of A vs B is flexible but both precede porting analyses. Corpus green + fuzz-sound at every
checkpoint (same ground rules).

**Move A — sever the AST dependency (coherence).** Litmus test gates the whole move.
- [x] **A1 IR-owned types.** `IrType` carries its own struct descriptor (interned name +
      copied field types/offsets); drop `struct_decl: Decl*` as an identity/name source. Give
      every scalar/aggregate a self-contained C-ABI layout (size/align) so the backend needs no
      AST.
- [x] **A2 IR-owned symbols.** Replace `IrInstr.aux.callee: Decl*` with an interned IR function
      symbol (name + `IrFuncSig`). Callee identity is an IR concept.
- [x] **A3 IR-owned literals/provenance.** (`src_decl`→opaque `void*`; names interned) `aux.str.bytes` → interned IR string pool. Every
      node keeps only `{line, col, interned-name?}` provenance — never a live AST pointer.
- [x] **A4 No AST tokens in the IR.** Verify no residual `TokenKind`/AST enums in `ir.h`
      (binops/cmps are already `IrOp`/`IrCmp` — audit).
- [x] **A5 GATE (litmus) ✓ PASSES:** `src/ir/*.h` and `src/analysis/*.{h,c}` compile with **neither
      `ast.h` nor `token.h` included**. Lowering (`lower.h`) is the ONLY AST-aware unit — it is
      the front-end→IR *adapter*, not part of the IR.

**Move B — verification-native IR (power + soundness).**
- [~] **B1 `IR_ASSUME` / `IR_ASSERT`** over an IR-owned predicate (linear-arith to start:
      `±x±y ≤ c`, `x==y`, `x≠0`; extensible). Guard refinement → `assume` on the taken edge;
      each obligation (bounds/overflow/div) → `assert`. The VRA becomes: propagate octagons,
      *consume* `assume`, *discharge* `assert`. **This deletes the `f->src_decl` refinement leak
      the VRA currently has** (refinements become entry `assume`s emitted at lowering).
- [x] **B2 Function contracts** `requires`/`ensures` on `IrFunc` (IR predicates). Call site:
      callee.requires ⇒ `assert` before the call; callee.ensures ⇒ `assume` after. Gives
      interprocedural bounds/refinements *soundly* (call-site precondition checking) — the piece
      the accept-side survey currently assumes the old engine enforces.
- [x] **B3 `IR_OPAQUE` / havoc** ✓ (c9a2eca) — declared read/write/effect footprint; the
      TOTALITY primitive. `incomplete` 60 → 13, faithfulness 88% → 97%, and precision went UP
      (576/612 → 587/622) because more code became reachable. Reserved now for unmodelled
      CONTROL FLOW only.
- [~] **B4 Refinements as first-class IR type structure** ✓ STATIC INTERVAL (3a9e366) —
      `IrType.has_refine/refine_lo/refine_hi`, read by `irtype_int_range` so bounds, overflow
      AND div-by-zero all tighten at once. `type Small = u8 < 200` was being DISCARDED when
      the alias resolved to its base. ★ The scope distinction that matters: a static interval
      is the same for every value of the type and belongs here; a slice's length and a
      region's extents are per-VALUE runtime quantities and belong in the IR. B4 is not
      "move everything onto types". REMAINING: predicates beyond an interval. Originally: the
      canonical-type keystone. VRA reads them from the IR type, never the AST. Unlocks dependent
      lengths (a symbolic-length slice with a *named* length identity) and the `i < a.len` class.
- [ ] **B5 Region + linearity qualifiers as IR type/fact structure** (prep for the borrow &
      linearity passes: lifetimes, mutability, use-once). No analysis yet — just the substrate.
- [ ] **B6 GATE:** VRA re-pointed to assume/assert/contracts; `f->src_decl` reads gone;
      accept+reject differentials ≥ current; fuzz 0-unsound.

**Predicate language (design item, write the annex):** a small IR-OWNED typed predicate AST —
linear arithmetic + equalities/disequalities now; aliasing/separation predicates for borrow;
use/linearity predicates. This is the lingua franca the assume/assert layer and every analysis
share. Lives in `src/ir/pred.h`; spec annex documents it.

## Phase 3 — Port the remaining analyses  ⟶ deliverable: retire `src/sema/`
      (now: every analysis reads IR + the assume/assert/contract layer — NO AST)

- [~] **3.1 Borrow / region** — **DECISION (session 52): BUILD a real NLL-class borrow checker,
      do NOT port the old one.** The old checker (~2.1 kLOC) is a known-weaker ceiling
      (two-phase only for direct-read args ⇒ `v.push(v.len())` rejected, E004); porting it
      would enshrine that ceiling and contradict IR sovereignty. Our IR is MIR-shaped, which
      is exactly where NLL belongs. **Full model in `internal/design/borrow_checker_design.md`.**
      Done so far (0bf7efa, 6019a08): dangling-return escape analysis (incl. struct-by-value
      fields), test_borrow.c 6/6, 0 false-pos across 340 pass programs — this becomes the
      degenerate case of the region rule.
      Sub-phases: **A** places+overlap, loans, liveness regions, the conflict rule (subsumes
      the ~12-test conflict cluster AND unifies move/leak as `Move` accesses); **B** two-phase
      borrows + reborrow chains (GAINS `v.push(v.len())` — a capability the old engine lacks);
      **C** cross-function via elision defaults + a language proposal for lifetimes;
      **D** ★ numeric-aided disjointness — query the octagon for `i ≠ j` / range-disjointness
      so `a[i]` vs `a[j]` and `a[0..k]` vs `a[k..n]` are provably disjoint: **a safe
      `split_at_mut` with no `unsafe`, which Rust structurally cannot do.**
      Requires the REAL B5 (places, loans, region vars, borrow kinds — design §8).
- [~] **3.2 Linearity / ownership** as an IR pass (2558562, 53d6e8c) — E001 use-after-move +
      E002 double-move (forward may-move CFG dataflow; `mov`→`IR_CONSUME`, store re-inits) +
      **E003 leak** (owned ptr/slice live at return) + **B5 linear substrate** (IrType.linear,
      set via a sema_type_is_linear wrapper). 0 false-pos across all 340 clean-loading pass
      programs; test_linearity.c 9/9; fuzz_linear 0 over-rej/0 missed. TODO: branch-consistency
      (E016), per-field/transitive-struct leak tracking.
- [x] **3.3 Effects** as an IR pass (8a7567f) — the effect LATTICE (F1) computed from the IR
      alone + call-graph fixpoint; `ir_lower_module` (bodies + extern stubs). Differential vs
      old `effect_full`: 0/362 drop an observable effect; DIVERGE 72× more precise (semantic).
- [ ] **3.4 Termination / measure** as IR analysis (loop + recursion, via the fixpoint).
- [ ] **3.5 GATE:** ~~full parity~~ → **ADJUDICATED DIVERGENCE** across the corpus + fuzzers;
      make IR pipeline the default; delete `src/sema/`; update docs.
      **The parity criterion was a structural mistake** (see `critical_retrospective.md` §2):
      "0 findings on programs the old engine accepts" *forbids the new engine from ever being
      stronger* — a better checker fails it by construction. Replace with: every divergence
      CLASSIFIED as `old-unsound` (win, add a fail-test) / `new-over-strict` (bug, fix) /
      `new-more-precise` (win, add a pass-test) / `new-unsound` (must be empty). Gate passes
      when the UNADJUDICATED set is empty, not when divergence is zero.

### ★ CORRECTIVE BACKLOG (from `internal/design/critical_retrospective.md`, session 52)
Each is a self-inflicted debt found by auditing the rebuild against its own goal:
- [x] **C1 Rework the gate to adjudicated divergence** ✓ (session 55; it earned its keep in
      57–58, where the `new-more-precise` and `front-end-policy` columns are what let two
      corpus errors and four surface-policy rules be classified honestly instead of chased).
- [x] **C2 `fuzz_ir_codegen.sh`** ✓ (teeth-verified: catches the scalar-var miscompile 4/40 when reverted; 200/200 clean with the fix) — execution-differential fuzzer for the NEW pipeline
      (compile through BOTH, diff behaviour). All 14 existing fuzzers test only the OLD
      pipeline; that blind spot hid a real miscompile (scalar `var` param by value, 41≠42).
      **Must exist before the IR pipeline is ever made authoritative.**
- [~] **C3 Measure + retire `incomplete`** — MEASURED ✓ (58) and largely RETIRED ✓ (60, B3): every site now names its
      reason (`IrFunc.incomplete_why`) and `ir_incomplete_survey.sh` ranks them —
      Now **524/537 (97%) faithful** after B3; 13 left (11 conversions + 2 control-flow).
      **★ The ranking is now the lowering work list, and it is dominated by ONE feature:**
      - ~~**ENUMS ≈ 42/103 (41%)**~~ ✓ DONE (session 59): IRT_SUM + the three ops, all
        construction forms and `case`. **incomplete 103 → 60, faithfulness 80% → 88%.**
        Remaining, re-ranked: 30 `unhandled-expr` (error unions — EXPR_TRY/EXPR_ELSE, 20 —
        plus array literals in expression position and match-as-expression), 7
        `unhandled-binop`, 6 `unhandled-builtin` (SIMD: movemask/load/store/shuffle, which
        need a vector type in the IR and are off the North Star), 4 `unresolved-member`,
        4 `unresolved-global` (global arrays). ★ Error unions are next by size but carry a
        caveat the design doc flags: their niche values are OBSERVABLE, so the new backend
        must reproduce the old niche search exactly, not merely implement a correct sum.
      - *(historical)* 37 `unresolved-member` were mostly enum VARIANT references
        (NotFound/None/Permission/Red…), plus 14 `enum-match`. **This is the single
        highest-leverage lowering item left, and it is DESIGNED but not built:**
        `internal/design/ir_sum_types.md`. The decision that matters: the IR models a TAGGED
        SUM (`IRT_SUM` + `IR_SUM_NEW`/`IR_SUM_TAG`/`IR_SUM_PAYLOAD`), and the old backend's
        NICHE ENCODING (`*u8|NotFound|Denied` is literally a pointer with markers at 0 and 8)
        stays a BACKEND layout choice. Modelling the niche in the IR would make a sum
        indistinguishable from a pointer with a strange range — destroying the very
        discrimination the analyses need, and repeating the exact
        representation-standing-in-for-semantics mistake found twice this session (string
        storage duration, ownership-vs-linearity). Expected payoff: incomplete 103 → ~61.
      - 35 `unhandled-expr` (the long tail: SIMD, fn-pointers, error monads)
      - 7 `unhandled-binop`, 4 `unresolved-global` (global arrays), 3 `unlowered-lvalue`,
        2 `aggregate-init`, 1 `unhandled-stmt`
- [~] **C4 Promote S2 ABOVE further VRA tuning** ✓ DONE as scheduling (60/61: B3 then S2,
      ahead of the remaining Stage-III divergences) and S2's CORE has landed (3b89944).
      Still open: the separation/disjointness predicate for borrow phase D.
- [~] **C5 Redesign the effect lattice** ✓ WRITE FOOTPRINT — `IrFunc.param_writes`, computed
      transitively, conservative for extern/opaque. Mutability in the conflict rule now comes
      from the FOOTPRINT, not the parameter type, which fixed BOTH directions: a `var` scalar
      the callee never writes is no longer a conflict, and a SLICE/STRUCT param (which never
      set is_mut at all, since only IRT_PTR carries ptr_mut) now does — closing whole-array
      and subslice co-arg aliasing. REMAINING: a READS footprint, and folding IO into
      "writes whose region is the external world". Originally: 3.3 shipped a port of the old
      `EffectSet`; the plan's own "one effect row" (reads/writes/raises/diverges + region
      footprints) is unmet. Re-derive it, don't inherit it.
- [ ] **C6 Type lattice as infrastructure** — refinements still live in ad-hoc side-maps
      (`slicelen`) instead of on `IrType`; B4/B5 deferrals have compounded every phase.
      **MEASURED GAP (session 51)** — the `--reject` seam (`g_suppress_ownership`) finally lets
      the new passes run on programs the old engine exit()s on, so reject-parity is now
      measurable: **ownership fail-tests CAUGHT 9 / 39**. The 30 missed need rules the old
      2139-line checker has and the new cores don't: conflicting/reborrow/NLL-loop borrows
      (borrow_conflict, cross_stmt_borrow, reborrow, nll_*), defer semantics (defer_consume,
      defer_double_*), move variants (move_needs_mov, move_then_use), co-arg aliasing
      (coarg_alias_*), owner_reassign/immutable. ⇒ 3.5 needs a real NLL borrow checker +
      defer + aliasing on the IR — a multi-session build, NOT a finishing touch. Note also
      the VRA is at 523/563 bounds; the old engine's ACCEPT set is matched (0 FP) but
      check-free coverage is not yet dominating.

## Phase 2.95 — Documentation deliverables (SCHEDULED, write FROM the stable engine)
- [ ] **Lain-IR spec** (`spec/` chapter + annex, normative) and **thesis** (`.tex`, expository)
      — one sovereign, verification-native IR. Outline + gating in
      `internal/design/IR_THESIS_SPEC_PLAN.md`. Write at end of 2.9 (IR feature-stable),
      refresh after Phase 3. Rule: docs track reality — never ahead of the code.
- [ ] **Front-end limits log** — `internal/design/lain_language_limits.md` (running): the
      surface language is lagging the IR's expressiveness (refinements far weaker than the
      assume/assert layer; no source `assume`/`assert`; refinement exprs untyped; narrow
      dependent lengths; coarse effects). Input to a future FRONT-END REWORK. Accumulate; the
      north star is "anything the IR can verify, the language should be able to state."

## Phase 4 — Backend & beyond  ⟶ deliverable: leverage the clean IR

- [ ] **4.1 Better C emission / IR-level optimization** (the recognizers become real opts).
- [ ] **4.2 (Optional) LLVM backend seam** on the IR.
- [ ] **4.3 Re-express niche opt / SIMD / annotations** on the IR.

---

# ★★ THE COMPLETE ENDGAME — master tracklist to the god-like language/IR/compiler
> Added session 53. **This section is AUTHORITATIVE for sequencing.** Everything above is the
> rebuild (Phases 0–4); everything here carries it to the actual end state. `GRAIL_PLAN.md`
> uses a DIFFERENT, older Phase 0–4 numbering for the *language* grail — it is retained for
> its rationale but **superseded for sequencing by this section**. When lost, read this.

## E0. What "done" means (the acceptance definition — no hand-waving)
The endeavour is complete when ALL of these hold simultaneously:
1. **Total IR** — no `incomplete` escape hatch; every construct modelled (`opaque`/havoc with
   declared footprints). Coverage measured, not assumed.
2. **Sovereign** — `src/{ir,analysis}/*` compile with zero front-end headers (structural, A5
   ✓) AND no primitive encodes front-end-specific policy (semantic litmus, audit ✓ — re-run
   whenever the lattice grows).
3. **Dominating** — for every corpus program the new engine's verdict is ≥ the old engine's:
   never unsound, never over-strict, strictly more precise on a measured set. `src/sema/`
   deleted.
4. **Maximal-power analyses** — numeric (relational + the nonlinear frontier), memory safety
   (NLL-class + numeric-aided disjointness), resource (linearity), effects (a designed
   lattice), termination (loops + recursion).
5. **Rich type lattice** — refinements, regions/lifetimes, linearity, effects carried ON
   types, not side-tables.
6. **Multi-language** — at least one non-Lain front-end lowers to the IR and gets the same
   guarantees, proving the sovereignty claim empirically.
7. **Proof-exploiting backend** — checks the IR proved away are absent from the emitted code;
   an LLVM/native seam exists.
8. **The language can state what the IR can verify** — lifetimes, refinements, assume/assert,
   effects are all expressible.
9. **Formal spine** — normative spec + thesis, written FROM the implementation.

## E1. Invariants that hold at EVERY step (violating one is a bug, not a tradeoff)
- **Corpus green** (`run_tests.sh` 625) and **behavioural gate green** (`diff_engines.sh`).
- **Never unsound**: the reject direction is checked before the accept direction.
- **Fail-closed** on anything unmodelled.
- **Every injected `assume` is paid for by a discharged `assert`** (the dual invariant).
- **Adjudicated divergence, never parity** (C1) — the gate must permit being *stronger*.
- **Design before code** for any new IR facility; apply the semantic-independence litmus.
- **Docs track reality** — never write the spec ahead of the engine.

## E2. Phase order (dependency-correct; each ends green)

### ▸ STAGE I — Unblock (do first, small, gates everything)
- [x] **C1 adjudicated-divergence gate** ✓ (3654003) — rework `phase3_differential.sh`. *Blocks all of
      Stage II.* Without it a stronger engine cannot land.
- [x] **C2 `fuzz_ir_codegen.sh`** ✓ (teeth-verified: catches the scalar-var miscompile 4/40 when reverted; 200/200 clean with the fix) — execution-differential fuzzer for the NEW pipeline.
      *Blocks E0.3/E0.7* (never make the IR authoritative without it).
- [x] **C3 measure `incomplete`** ✓ — `ir_coverage.sh`. **MEASURED: 82% (533/648 functions;
      115 incomplete)** — every metric reported so far (523/563 bounds, 73 fully proven, the
      340-program 0-FP scan) is conditioned on that denominator. The gap is CONCENTRATED and
      actionable: **error handling (`try`/`else`/propagate), enums+match+niche, and `defer`**
      — i.e. the "IR is BEHIND the language" gaps (`lain_language_limits.md` §7). Feeds
      Stage II B3 directly: model them (ADTs, error unions, defer) or give them declared
      `opaque` footprints; either way `incomplete` must reach 0.

### ▸ STAGE II — The IR core reaches full power
- [~] **S2 rank-N strided regions** ✓ CORE (3b89944) — `IR_SHAPE` states a flat region's
      extents (runtime values, so NOT on IrType); row-major strides derived. Bounds FACTOR
      (`i*w+j < h*w` nonlinear → `i<h ∧ j<w`, both already in the octagon) and the
      intermediates' overflow discharges from the same fact. matrix_symbolic_2d 15/21 →
      18/21. Negative cases unit-pinned (widen either guard ⇒ refused). **Confirmed the
      retrospective: the 2D case was a MEMORY-MODEL gap, never a numeric one.**
      REMAINING: rank > 2, shapes on locals (not just dependent params), and the
      disjointness predicate the borrow checker's phase D needs.
- [~] **B5-real: places ✓ (ae8a859)**, loans, region vars, borrow kinds
      (`borrow_checker_design.md` §8). `ir/place.h` — rooted access paths + the overlap
      relation that the conflict rule and definite-assignment both reduce to; distinct
      roots/fields provably disjoint, indices delegated to the VRA seam
      (`ir_place_index_disjoint_fn`) — the ★ beyond-Rust hook. test_place.c 12/12.
- [ ] **B4: refinements ON `IrType`** (C6) — retire the ad-hoc `slicelen` side-map.
- [x] **B3: `opaque`/havoc with declared footprints** ✓ COMPLETE — `incomplete` 60 → 13 → **2**
      (99% faithful); the 2 left are unmodelled CONTROL FLOW, which an opaque cannot express.
      Originally (c9a2eca): `incomplete` 60 → 13,
      faithfulness 88% → 97%; precision went UP (576/612 → 587/622). Remaining 13: 11 more
      conversions (unlowered-lvalue/unresolved-global/aggregate-init) + 2 genuinely
      unmodelled CONTROL FLOW, which an opaque cannot express.
- [ ] **C5: redesign the effect lattice** — reads/writes/raises/diverges + region footprints;
      re-derive, don't inherit.

### ▸ STAGE III — Analyses to maximal power
- [x] **3.0 DEFINITE ASSIGNMENT** ✓ (aa49b0c, cce3e3e, 474aa6f) — depth-2 field paths AND
      per-element arrays; the divergence column is EMPTY. CORE (aa49b0c) — `analysis/definite_init.h`, a forward
      MUST-init dataflow on the place lattice. **8 of 15 divergences closed, 0 false
      positives, gate holds.** Remaining 7 sub-classified in the ledger: undeclared
      identifiers (resolve-level, not dataflow), per-element array init (needs the VRA to
      prove a fill loop total — same bridge as a[i]/a[j]), nested field paths (needs a path
      trie). Original note:
      **A whole analysis the plan had MISSED** — surfaced by the C1 gate (15 divergences):
      the old `linearity.h` quietly implements it alongside ownership, so "port linearity"
      silently meant "port two analyses". A forward must-init dataflow over the CFG with
      per-place init state — shares the place lattice with the borrow checker, so build it
      on the same substrate (design §1.1), not as a third bespoke pass.
- [~] **3.1-A/B/C/D borrow checker** — **the borrow divergence column is EMPTY (9c1564a).**
      Scoped borrows (IR_BORROW/END, a CFG reachability region) closed the last one.
      **D ✓ (aded6fe): the octagon answers a[i] vs a[j].**
      `ir_place_index_disjoint_fn` filled; conservative by default, the bridge only ever
      REMOVES a provable non-conflict. Relational case works (`a[p]` vs `a[q]` under `p<q`,
      neither index valued) — the case Rust cannot express. Wired into the CO-ARGUMENT rule;
      the liveness-region path still uses the conservative default. A/B/C: NLL core → two-phase →
      cross-fn elision → **★ numeric-aided disjointness (beyond Rust: safe `split_at_mut`)**.
- [x] **3.2 linearity completed** ✓ (a6f3218) — consumption is now ONE rule, `escape = move`:
      returned to the caller, built into an aggregate, or passed to a parameter the callee
      OWNS (the callee's mode is what keeps it precise — `write_file(f,s)` borrows). Move-on-
      assign is that same rule rather than a separate one. Plus **existence** (a resource has
      no obligation at a return it never reaches — MAY for E003, MUST for E016) and **sum
      payloads** (destructuring MOVES the resource out of the scrutinee into the arm's
      binding; per-variant reasoning falls out of the CFG). E016/per-field were already there.
- [x] **3.4 termination** ✓ — loop measures (session 21) + RECURSION measures on the IR
      (well-founded parameter, asked at the call edge). Closes the DIVERGE-vs-measure gap.
- [~] **VRA frontier** — 754 → **766/785** bounds obligations, 0 false proofs. Landed: inferred
      CALL RETURN RANGES (read off the callee's body), module-constant folding (a mangling
      mismatch meant NO module constant ever folded), division relational facts, the MIDPOINT
      identity derived by substituting a subtraction's definition into an octagon fact, stack
      VLA lengths, and **struct `in` field invariants** (assert at construction, assume at the
      read). Remaining: SIMD lanes, `in`-condition/keyword forms, and the accumulation-overflow
      honesty pass. ★ **fuzz_vra.sh** now gives the new engine's proofs teeth for the first
      time — it executes what the engine proved, under ASan.
- [x] **2.2 variable packing** ✓ — only values that can hold a numeric relation get an octagon
      dimension; constants live in the constant table instead. **800x** (4028ms → 5ms on a
      128-element literal) AND more precise. Exposed a latent unsoundness in `oct_widen_sel`.
- [~] **3.5 GATE — the OWNERSHIP half is DONE** (4d449b1, b09dbe1). `--engine=ir` makes the
      sovereign analyses authoritative in the REAL compiler (`engine_ir_gate.sh`):
        392 pass programs accepted · **0 false positives** · 235 fail programs caught
      and all 9 it does not catch are already adjudicated — five corpus corrections (C-1…C-5),
      two C-3 policy, two front-end spelling rules (`mov`, `var`). Nothing unaccounted for.
      **The NUMERIC half is not ready and is now measured honestly:** `--engine=ir-full` has
      114 false positives, and they are three different things, not one — old-engine
      fail-opens (struct-field overflow: the old engine ACCEPTS `p.x + p.y`, widens to int64
      and narrows, and silently returns −2147483648 for 2147483647+1), the accumulation
      honesty question, and array-element/struct-field value tracking.
- [ ] **DELETE `src/sema/`** — blocked on Stage IV: the analyses are ready to be authoritative,
      the BACKEND has not earned the proofs. Deleting sema means the old emitter goes too.

### ▸ STAGE IV — The backend earns the proofs
- [~] **4.0 CAN THE NEW EMITTER REPLACE THE OLD ONE?** — `emit_gate.sh` runs the real corpus
      through BOTH backends and compares stdout + exit code. Start: 209 agree / 15 differ /
      **141 could not build**. Now: **321 agree / 22 differ / 24 cannot build.**
      ★ Almost none of the gap was backend immaturity. It was SEVEN front-end and lowering
      defects the old path happened to paper over, each found by running the corpus rather
      than by reading code:
        · string escapes never DECODED — `.len` wrong for every literal containing one
        · externs lowered to a NAME with no signature — no prototypes, no variadics, so every
          printf call was an implicit declaration (102 of the original 141 failures)
        · `.data` on a slice never lowered — the idiom the whole C boundary is written in
        · `*p = v` through a raw pointer silently LOST
        · integer literals truncated to i32 (`var x i64 = 5000000000` → 705032704)
        · **error handling** — `T | m1 | m2` had no type lowering, so `try`/`else` had nothing
          to test; the last of C3's three clusters (error handling, enums/niche, defer)
        · **floats** — IRT_FLOAT and aux.fimm existed from the start and NOTHING ever produced
          one; f32/f64 were not recognised as type names
        · a value binding (a match payload) had no ADDRESS, so `p.x` on one dereferenced null
        · **`defer` is BLOCK-scoped**, and the lowering asserted in a comment that it is
          function-scoped — every nested defer ran too late and in the wrong order, and
          `break`/`continue` skipped theirs entirely. Not only codegen: the linearity pass
          reads defer, so its SCOPE decides whether `defer drop(mov r)` is a release or a
          double one.
        · the case EXPRESSION (`return case o { Some(v): v  None: d }`) was not lowered at all
        · a global constant ARRAY read dereferenced null (it cannot fold to a scalar, so it
          fell through to OPAQUE)
        · **SIMD vectors were not in the type lattice** — `Vec(N,T)` lowered to `void*`, the
          largest remaining build-failure class. `IRT_VECTOR` (f093e30) says only "N lanes of
          T"; the vector_size typedef is the backend's choice, as tag-vs-niche is for a sum.
        · **function pointers** — `IRT_FUNC`, `IR_FUNC_REF` and the INDIRECT form of IR_CALL
          (f99a9aa). The old code nulled the callee decl and fell back to the identifier's
          NAME, so the emitted C called a nonexistent function while the analyses resolved
          that name to nothing — the right answer for the wrong reason.
        · **union WIDENING** on the `try` path (ad457db) — the same marker at a different
          variant index; and `IR_TERM_UNREACHABLE` emitted `return 0`, a type error the moment
          a function returns a struct. Unreachable code still has to be well-TYPED.
        · **WIDE ACCESSES** (b662310) — the SIMD builtins turned on one honest question, not
          plumbing: a 32-lane load at `i` reads a[i..i+32), so `i < len` is the WRONG
          obligation. `IR_ELEM_PTR` records the access WIDTH (1 for `a[i]`), which is not
          SIMD-specific — a bulk memcpy-shaped access is the same fact — and the existing
          numeric analysis then decides it with no SIMD reasoning at all.
      **Now: 369 agree / 3 differ / 0 cannot build — the new backend builds and runs the
      whole corpus.** The last three differences are a comptime `if` that is not evaluated, an
      ADT parameter match, and direct ADT field access under `unsafe`.
      Session 76 closed the remaining twenty-four, and they were LOWERING gaps and front-end
      defects rather than backend immaturity — including one build failure that was HIDING a
      miscompile (a short-circuit `while` condition branched from the loop header, an infinite
      loop nothing could see because the only program with that shape would not build), a
      stack VLA with an assumed length and NO STORAGE, and an array field that did not decay.
      Earlier in the session: 343 agree / 22 differ / 7 cannot build. The seventeen build failures closed in
      session 75 were, again, LOWERING gaps rather than backend immaturity: generic templates
      lowered (a `unit` VALUE parameter for the type param), an inferred generic call losing
      its result, an EMPTY body treated as no body (`func drop(mov {id} R) { }` — the
      idiomatic destructor — emitted nothing and every call to it was an undefined symbol),
      variant construction with no slice→pointer coercion, and optional NARROWING (`if x` over
      `*u8 | none`) never lowered at all.
      Remaining 7: string equality on slices, a SIMD lane-signedness mismatch, a struct slice
      field, an int alias reaching `double`, two variadic printf argument types, and a
      refinement type name. Remaining 22 differences have no dominant cause.
- [~] **4.1/4.3** proof-exploiting C emission. **Annotations DONE** (`src/ir/annot.h`):
      `restrict` from the borrow checker's exclusivity — measured at 12 instructions vs 33 on a
      vectorizable kernel, because gcc otherwise versions the loop and tests for overlap at
      runtime — plus `pure`/`const` from the effect row AND the write footprint (the row's
      WRITE bit means GLOBAL state by design, so it alone would have annotated a function that
      mutates both its arguments). `IrType.is_raw` distinguishes a borrow from an unsafe
      pointer, which is what makes `restrict` a theorem rather than a guess.
      Still open: proven RANGES as `__builtin_unreachable` assumptions, alignment, trip counts;
      niche/SIMD re-expressed on the IR.
- [ ] **4.2 LLVM/native seam** on the sovereign IR (E0.7).

### ▸ 3.5b — THE NUMERIC HALF (session 77–78)
- [~] `--engine=ir-full` false positives **115 → 60**, catches 243 → **245**. The number was a
      BUCKET before it was a fact: nobody had separated "the new engine is imprecise" from
      "the old engine is unsound", and the second half turned out to be the bigger one.
      ★ **~50 of the 60 that remain are the ACCUMULATOR class (C-6), where the new engine is
      RIGHT** — the old engine's overflow check does not survive a loop (`while i < n
      { s = s + i }` at u8 prints 188 for a sum of 19900). Those programs are real overflows
      and need a guard, a wider type, or `+%`; that is the language's promise arriving.
      Fixed on the way: the overflow obligation belonged to the RESULT type (Path-F widens),
      which required BUILDING the narrowing obligation first; block ids are not control flow
      (DFS back edges + natural loops — five programs' fixpoints DID NOT CONVERGE, including
      the flagship binary search, and the final pass was discharging against the partial
      result); the fixpoint now FAILS CLOSED if it exits through the sweep cap; a termination
      measure need not step by a constant; `d != 0` is a fact no interval can hold, so ask the
      CFG; `unsafe` waives the numeric obligations and the region is the whole block; four
      opcodes with an exactly-known result were being forgotten; and widening now climbs a
      THRESHOLD LADDER of the program's own constants (Astrée-style).
      **Next: a `--dump-octagon` state instrument.** Three precision questions in a row have
      been answered by bisecting programs instead of reading the abstract state, and the
      binary-search midpoint's overflow obligation is stuck exactly there — see
      `lain_language_limits.md` for what bisection already established about it.

### ▸ STAGE V — The language catches up to the IR
- [x] **F1 lifetimes/regions syntax** ✓ **BUILT** (design + implementation) — and the framing was wrong
      (`internal/design/f1_lifetime_syntax.md`). Rust needs lifetime parameters because it
      checks each function against its SIGNATURE ALONE; Lain-IR lowers the whole module and
      `bor_ret_borrow_mask` INFERS which parameter a returned reference borrows, from the body
      — which Rust declines to do at all, rejecting the ambiguous signature outright. For every
      function with a body the annotation would be REDUNDANT and less precise than what the
      engine already has. Inference reaches everywhere except the EXTERN boundary, and looking
      for that found a live SOUNDNESS HOLE rather than an expressiveness gap (3e1198d): an
      extern returning a reference lent nothing at all. Proposed: `... var i32 in a`, one
      clause reusing the existing `in` keyword — believed on an extern (that is what `extern`
      means), CHECKED against the inferred mask on anything with a body, because an annotation
      the compiler trusts and never verifies is defect D-4. Roughly F2's size, not a lifetime
      system. Superseded framing kept in `lain_language_limits.md` §6b with a pointer.
      **BUILT**: the clause parses on both function and extern declarations, REPLACES the
      fallback on an extern, and is CHECKED against the inferred mask on anything with a body
      (E124 when it claims less than the body does — the inferred mask is kept, soundness is
      not negotiable). New-engine-only precision for now: the old borrow checker does not read
      the clause. Two corpus tests; all four directions verified.
      ORIGINAL entry (`lain_language_limits.md` §6b) — was called the flagship gap;
      unblocks cross-function borrow precision.
- [~] **F2 predicate syntax** — `assert(pred)` / `assume(pred)` ✓ (4c5ad42, limits §2): the IR
      had both primitives from the start and the language could say NEITHER. `assume` is
      confined to `unsafe` (E123) — an unchecked annotation the prover trusts is defect D-4 by
      construction — while `assert` is always allowed, because an obligation is not a licence.
      BOTH engines learn from it (the old range table applies an `assume` as it applies a
      guard), without which the feature would be inert in the authoritative one.
      REMAINING in F2: arbitrary linear-arith predicates, relational
      preconditions, `assume`/`assert` statements (limits §1, §2).
- [x] **F3 effect annotations** ✓ — `effects io, alloc` declares an UPPER BOUND on the row
      (write/diverge/raises/io/alloc); `effects` with an empty list means PURE. Same rule as
      F1: believed on an extern, CHECKED against the inferred row with a body (E125 when it
      declares fewer effects than the body has; declaring more is imprecise and allowed).
      Checked in the OLD engine against `effect_full`, so unlike F1's precision this is not
      new-engine-only.
- [x] **F4 type-check refinement exprs** ✓ (c01dca6) — and the defect was DEEPER than its
      title: they were never RESOLVED either, so identifiers in them carried no binding.
      Resolve then infer, in a SECOND pass over the parameters (a constraint may name a LATER
      parameter). Verified by DELETING the IR workaround that existed only for this: the
      bounds survey is identical with and without it. **The first Stage V item to land.**
- [ ] **F5 front-end rework proper** — lexer/parser/AST, if the above justify it.

### ▸ STAGE VI — Prove the sovereignty claim empirically
- [~] **M1 a second front-end** ✓ CORE (a1b0b0d) — `src/frontends/cmin`, ~330 lines: its own
      lexer, parser and symbol table, building Lain-IR directly and including ONLY `ir/*` and
      `analysis/*` (zero references to ast.h/sema.h/parser.h — so the BUILD would have said
      sovereignty was false, rather than a document claiming otherwise). `cmin_gate.sh`: the
      same analyses catch E085/E005/E087 in C and prove the guarded shape check-free, and a C
      program compiled THROUGH Lain-IR returns what gcc returns.
      ★ **PARITY is the sharp result**: the same program in Lain and in C produces the same two
      findings, including the same two LIMITATIONS — the precision frontier is in the IR, not
      in either front end. ★★ **And nothing in the IR had to change** to accept it.
      Remaining: the subset does not exercise the OWNERSHIP analyses (C has no `mov`), so
      numeric + memory-safety sovereignty is demonstrated and RESOURCE sovereignty is still
      argued. A Fortran-shaped front end (column-major regions) would test the region model
      the way this tested the numeric one. Verdict written up in
      `internal/design/sovereignty_second_frontend.md`.
- [ ] **M2 adversarial re-audit** — re-run the semantic-independence litmus against Fortran +
      Rust once the lattice is complete.

### ▸ STAGE VII — The formal spine
- [ ] **D1 normative spec** (`spec/` chapter + annex) — grammar, static/dynamic semantics,
      the assume/assert contract, per-pass soundness statements.
- [ ] **D2 thesis** (`.tex`) — the argument, the octagon, the methodology, the results, and
      the comparison (LLVM / MIR / CompCert / Viper). Outline in `IR_THESIS_SPEC_PLAN.md`.
- [ ] **D3 (stretch) machine-checked core** — the domain's γ-soundness in a proof assistant.

## E3. Navigation (for a long session — read this when lost)
| question | file |
|---|---|
| what do I do next? | **this section**, top unchecked box in the earliest open stage |
| why does the IR look like this? | `ir_semantic_independence.md` (+ the REFRAME above) |
| how does the borrow checker work? | `borrow_checker_design.md` |
| what did we get wrong? | `critical_retrospective.md` |
| what can't the language say? | `lain_language_limits.md` |
| what's the state of everything? | STATUS LOG (bottom of this file), newest first |
| how do I validate a change? | E1 invariants + `run_ir_tests.sh` + `phase3_differential.sh` |

---

## Key decisions log
- 2026-08-28 — **Rebuild in-place, same repo, new core beside old.** Reuse frontend + corpus;
  differential bring-up; no big-bang. (Decision rationale: the corpus is the safety net and
  must not fork; the frontend/language are fine; coexistence needs one repo.)
- 2026-08-28 — **Target domain = octagons + variable packing, over ℤ with overflow split,
  fixpoint over a typed SSA/CFG IR.** (Staging: clean closed DBM first is acceptable.)
- 2026-08-28 — **Spec split:** contract in spec ch.13 (done); octagon design annex written
  from the real engine at end of Phase 2 (`design/vra-octagon.md` is the interim home).
- 2026-08-28 — **AST is NOT rewritten (scope boundary).** A tagged-union parse tree is the
  right shape; the parser is kept. Its only real smell is analysis state bolted onto nodes
  (`l3_*_dead` on ExprBinary; `refine`/`canon`/`int_width_cache`/`arith_widened` on Type) —
  those die with `src/sema/` in Phase 3, and lowering already bridges AST Type → fresh IrType
  so the fatness stops mattering. Optional cheap follow-up: slim the Type struct in Phase 3.
- 2026-08-28 — **Domain staging locked (0.6):** closed **difference-bound** domain first
  (subset: `vᵢ − vⱼ ≤ c`; the recognizer catalog shows ~all current cases are DBM-only), then
  generalize to full **octagons** (`±vᵢ ± vⱼ ≤ c`) for the relational frontier (merge/interleave
  `i+j<len`). CFG + domain-join at merges first; SSA/mem2reg a precision follow-up. This
  de-risks the fixpoint engine bring-up while keeping the octagon endpoint.

## Risks & mitigations
- *Rewrite death-march* → phased, corpus-gated, coexistence + differential testing.
- *Octagon cost blowup* → variable packing; DBM-first staging.
- *Lost soundness cases* → the ~70 recognizers become the acceptance checklist (0.4);
  fuzz every analysis change.
- *IR infidelity* → Phase-1 gate proves the emitted C runs identically before any analysis.
- *Scope creep into language redesign* → OUT of scope; language is settled.

---

## STATUS LOG (update every session — newest first)
- **2026-09-07 (77)** — **★★ THE NUMERIC HALF OF 3.5, TRIAGED — 115 false positives → 63, and half of what remains is the new engine being RIGHT.** `--engine=ir-full` rejected 115 programs the old engine accepts, and that number was a BUCKET, not a fact: nobody had separated "the new engine is imprecise" from "the old engine is unsound". Separating them is what this session did, and the second half turned out to be the bigger one. ★ **The first thing triage found was not a precision gap but a WRONG OBLIGATION.** `func pure_add(a i32, b i32) i64 { return a + b }` was refused as not provably free of overflow. It provably cannot overflow: Lain's `+` is Path-F, it WIDENS, and the lowering says so (`add %0, %1 : i33`). `vra_check_overflow` was comparing against the OPERAND type, demanding an i32+i32 fit in i32. The obligation belongs to the RESULT type — and moving it required BUILDING PATH-F'S OTHER HALF first, because without a narrowing check the change would have been a fail-open. `vra_check_narrow` states it at a store into a narrower cell and at a truncating cast, and the case that proves the pair is the old engine's documented unsoundness: `proc addp(p P) i32 { var s i32 = p.x + p.y }` with `P(2147483647, 1)` — **the old compiler accepts it and PRINTS −2147483648**. Nothing is lost at 64 bits: there is no u65, so a u64 add's result type IS u64. ★★ **C-6, the largest finding, is a verdict on a CLASS of ~50: the old engine's overflow check DOES NOT SURVIVE A LOOP.** `while i < n { s = s + i }` with `s` a u8 and n 200 — the true sum is 19900, the old compiler accepts it and the program prints **188**; at i32 it prints 704982704 where the answer is 4999950000. Its loop widening clamps the accumulator's range to the type it is stored into, after which the store "fits" by construction — circular, so **no loop-carried accumulator has ever actually been checked**. Straight-line `var s i32 = a + b` it does reject; a loop defeats it. Those ~50 corpus programs contain an unbounded accumulator while testing something else entirely; each is a real overflow. Pinned by a pair, with the GUARDED form proving today and the naturally-bounded form recorded as the open frontier — bounding `s` by the loop's TRIP COUNT is a product, not a difference, and outside an octagon. ★★★ **And underneath all of it: BLOCK IDS ARE NOT CONTROL FLOW.** Two places assumed "Lain's CFG is structured, so a natural loop is a contiguous id range", and both said so in a comment. That stopped being true the moment `case` and `try` began allocating their JOIN block before their arms — the arms have higher ids, so every match join looked like a back edge. **Sixteen corpus programs with no loop in them at all were rejected as non-terminating**; the domain WIDENED at every match join, discarding what it knew there; and worst, the widening SELECTOR used the same id range, so a variable modified inside the loop but outside that range was NEVER WIDENED — its bound grew forever and **the fixpoint DID NOT CONVERGE on five programs, including the flagship binary search and the tokenizer**, which were exiting through the sweep cap with a PARTIAL fixpoint that every proof then rested on. (`binary_search_in_loop_pass` did not converge before this session either — the heuristic was already wrong and nothing checked.) Fixed in the order it has to happen: a loop header is the target of a DFS BACK EDGE; a loop body is its NATURAL LOOP (used by the widening selector AND the termination recogniser, failing conservative); and the fixpoint now **FAILS CLOSED** if it exits through the cap, because an under-approximation is not a state to discharge obligations against. With the natural loop in place all five converge, so the fail-closed net currently fires on nothing — which is the point. ★ Precision landed alongside, each item a fact the engine already had and was not asking for: a termination measure need not step by a CONSTANT (`b = a % b`, `n = n / k`, `n = n >> k`, `x = x & (x−1)` — every one justified by an interval the domain already derives); `d != 0` is a fact no interval can hold, so the division check asks the CFG instead (a block reached only through `if d != 0`, or only through the false edge of `if d == 0`) and the same fact as a PARAMETER REFINEMENT was not lowered at all; and four opcodes with an exactly-known result were being forgotten (negation — exact, and the octagon's own shape; a sum's discriminant in [0, variants−1], which `ir.h` names as the reason discrimination is exact; `@movemask` as [0, 2^lanes−1]; a bool as [0,1]). ★ **And a fuzzer had NO TEETH for a commit.** `fuzz_bsearch` went to `accepted=0 rejected=300` — which reads exactly like "the engine proves nothing", and I reported 0 unsound from it. Its generator calls `libc_printf` without declaring it, and E127 turned that into a parse-time rejection: every program failed before the numeric engine saw it. It now checks WHY it was rejected and shouts `GENERATOR BROKEN` on anything but its own diagnostics; every other fuzzer was checked for the same disarming. corpus **654/654**, --engine=ir-full **63** false positives (from 115) with catches 243 → **244**, --engine=ir 400 accepted / 0 false positives / 240 caught, reject survey 36 / 0 PROVED-ALL, VRA 833/846, trust 40/0, fuzz_{vra,div,loop,bsearch,ir_codegen} clean, IR units ALL, phase3 + emit gates hold, warnings NONE.
- **2026-09-07 (76)** — **★★ THE NEW BACKEND NOW BUILDS AND RUNS THE WHOLE CORPUS. `emit_gate` 326 / 22 / 24 → 369 / 3 / 0.** Deleting `src/sema/` takes the OLD EMITTER with it, so this gate is the finish line, and the twenty-four programs the new backend could not build turned out — again, and this is now a pattern worth naming — to be **LOWERING gaps and front-end defects, not backend immaturity**. ★ **D-16 is the one that matters most, and it is the worst kind of hole: `totally_made_up_fn(42)` COMPILED.** Nothing declared it, the emitted C leaned on C's implicit-declaration rule (removed as an error in C23), and every analysis resolved the name to nothing and treated the call as OPAQUE — that is, as permissively as possible. A typo produced a link error at best and a silently unanalysed program at worst. Now [E127]. Six corpus tests leaned on it, and fixing them made each SHARPER: four ownership fail-tests were checking name resolution rather than ownership, and now report the E007/E001/E008/E003 their headers claim. ★★ **And a build failure was HIDING A MISCOMPILE.** A `while` whose condition SHORT-CIRCUITS branched from the loop HEADER instead of from where the condition finished — `while i < n and p(a[i])` lowers its `and` into its own blocks and leaves `c->cur` at their join, so setting the terminator on `head` overwrote the edge INTO those blocks and the loop branched on a value nothing had computed: an infinite loop, silently. Invisible for as long as the one corpus program with that shape could not be built, so it was never run. Two more of the same family: **a stack VLA had an ASSUMED LENGTH AND A NULL DATA POINTER** — the prover knew `a.len == n` while nothing allocated anything, so `a[i] = 1`, an access it had just PROVEN in bounds, wrote through null (`IR_ALLOCA` with a COUNT operand is now a dynamic frame allocation, and the slice over it carries the length as a VALUE so nothing is assumed at all); and **an ARRAY FIELD did not decay**, so `m.data[i]` read a POINTER out of the array's first two elements and segfaulted. ★ The rest, each a language feature the new pipeline simply did not compute: generic TEMPLATES being lowered at all (a type parameter as a `unit` VALUE parameter — one cause, every generic program); an INFERRED generic call losing its RESULT (what a call produces is the callee's SIGNATURE, not the call site's inferred type); an EMPTY BODY treated as no body (`func drop(mov {id} R) { }`, the idiomatic destructor, emitted nothing and every call to it was an undefined symbol); **OPTIONAL NARROWING** in both directions — `if x` over `*u8 | none` and the `else` arm of a `case`, because exclusion is narrowing too; **NESTED VARIANT PATTERNS**, where `Err(Permission)` dispatched as `Err(NotFound)` and the test printed `BAD` while exiting 0; **CHECKED and SATURATING arithmetic**, all three of `+? as? +|` lowering to OPAQUE so the programs printed 0 where the language promises 255 — and a checked operator has no union to test, so the lowering BUILDS the overflow test, which means the numeric domain sees a real comparison and can prove the `else` arm dead; **ARRAY VALUE SEMANTICS** in both directions, plus a comprehension over a non-zero range that filled the wrong slots while `IR_INIT` stated it had filled them all; `IR_SEQ_EQ` for slice equality (memcmp in C, `slice ==` in Rust, `mem.eql` in Zig — an OPAQUE would have suppressed every proof in the function and a lowered LOOP would have invented a termination obligation); a Lain name that is a C KEYWORD; the `float` spelling; and the vector-mask SIGNEDNESS gcc chooses. Two details nearly went wrong and are recorded where they happened: `expr_is_checked_op` is true for a checked CAST as well as a binary op, so reading `.as.binary_expr.left` off an `as?` node SEGFAULTED the lowering; and widening must follow the OPERAND's signedness, or the fits-test answers about a different number than the program computes. ★ `check_build_warnings.sh` earned its keep within the minute: a bare `break` in my new array-literal case ran off the end of a non-void function — the THIRD time that switch has grown that exact undefined behaviour. corpus **650/650**, emit gate **369 / 3 / 0**, VRA **833/846** with 103 programs fully proven, reject survey 36 refused / 0 PROVED-ALL, --engine=ir 397 accepted / 0 false positives / 240 caught, IR units ALL, fuzz_vra + fuzz_ir_codegen 0, phase3 + cmin gates hold, warnings NONE.
- **2026-09-07 (75)** — **★★ THE BACKEND EARNS THE PROOFS (Stage IV 4.1), and the last 17 build failures were lowering gaps.** `src/ir/emit_c.h` emitted NO annotations — no `restrict`, no `pure`, no `const` — so deleting `src/sema/` would have taken every one of them with it and the analyses' facts would have stopped at the C boundary, the place they are worth the most. `src/ir/annot.h` is that boundary, computed from the IR alone. ★ **`restrict` is the one that pays, and here it is a THEOREM rather than the programmer's word**: the borrow checker has already refused every program in which two reference parameters could name the same object, and a SHARED borrow qualifies too — restrict's requirement bites only on MODIFICATION (C11 6.7.3.1p4), which a shared borrow forbids. MEASURED on `out[i] = a[i] + b[i]` over 1024 elements: with it, one vectorized loop, **12 instructions, 47 bytes of .text**; without, *"loop versioned for vectorization because of possible aliasing"* — two loops plus a runtime overlap test, **33 instructions, 117 bytes**. A slice is passed as a by-value struct so the PARAMETER cannot carry the qualifier; the pointer every access goes through is the `IR_SLICE_DATA` value, and qualifying its declaration says the right thing over exactly the right block. ★★ **Three things the work turned up, each worth more than the annotation.** (1) `pure` was being handed to a function that MUTATES BOTH ITS ARGUMENTS — and the effect row was RIGHT to say `effects == 0`: `IR_EFFECT_WRITE` means "writes mutable GLOBAL state" and says so, because a `var`-parameter write is part of the function's INTERFACE, visible to the caller through the argument. That is the right lattice and the wrong gate for gcc's `pure`, which forbids every memory write. The WRITE FOOTPRINT is the other half; both are now required. (2) The IR **could not tell a BORROW from a RAW POINTER** — `IrType.is_raw`, because the distinction is not Lain's: a borrow carries an exclusivity guarantee some checker discharged, a raw pointer carries nothing. (3) `fuzz_ir_codegen.sh` compiled at **-O0 only**, so it structurally could not see the class annotations introduce — an annotation is a promise the OPTIMIZER acts on. It now runs -O0 vs -O3 with two new generators for the falsifying shapes. TEETH: emitting `restrict` on raw pointers too — **36 MISCOMPILES over 250 programs**, the minimal case printing 11 at -O0 and 1 at -O3. ★ **Honest about what the attributes are worth**: in a single TU with every body visible, `pure`/`const` are INERT — gcc derives the same facts and its own IPA overrides the attribute (a `const` function that prints still prints at -O3, one that aborts still aborts). They are kept because they are correct by construction and go live the moment emission splits across TUs, under -flto, or on the LLVM seam as `readnone`/`readonly`. ★ **Then the emit gate: 326 → 343 agree, 24 → 7 cannot build**, and *none* of the seventeen was backend immaturity. **Generic TEMPLATES were lowered** — a template has no runtime existence, only its instances do, so lowering it produced `void* pick(void, void*, void*)`, and that one cause was every generic program in the corpus. An **INFERRED generic call lost its RESULT** (what a call produces is the callee's SIGNATURE, not the call site's inferred type — monomorphization renames the callee and leaves the expression's type unresolved, so a `const 0` placeholder took the result's place). An **EMPTY BODY is still a body** — `func drop(mov {id} Resource) { }`, the idiomatic Lain destructor, parses to a NULL statement list, so the function was skipped ENTIRELY and every call to it was an undefined symbol; lowering it then exposed the rule that made those tests pass, that destructuring a parameter IS the disposal. **Variant construction had no representation coercion** (`OptionByte.Some("hi")` put a two-word slice in a `*u8` payload — wrong at the ABI, not merely ill-typed). **OPTIONAL NARROWING was not lowered at all**: `if x { use_it(x) }` over `*u8 | none` branched on the whole SUM and passed the whole SUM to a callee declared `*u8`; an optional-shaped sum is a property of the TYPE and not a Lain spelling (Rust's Option, Zig's `?T`, a nullable pointer), so `if` now tests the tag and rebinds the name to the payload — which is what the front end's flow-narrowing MEANS, said in the IR. ★★ **And unblocking those exposed a silently WRONG DISPATCH**: `case r { Err(NotFound): … Err(Permission): … }` matched only the outer variant, so every `Err(...)` arm took the first — the runtime test printed `r2=NotFound BAD` and exited 0. Nested variant patterns are a LOOKUP, not a syntax rule (does this identifier name a variant of the payload's own sum type?), so one helper serves the `case` statement and the `case` expression, which had the same hole and would have grown a second copy of the bug. corpus **650/650**, emit gate **343 / 22 / 7**, --engine=ir **397 accepted / 0 false positives / 241 caught**, IR units ALL, fuzz_ir_codegen 0 miscompile, phase3 + cmin gates hold, build warnings NONE.
- **2026-09-07 (74)** — **★★ OWNERSHIP NOW PAYS FOR NUMERIC PRECISION — the alias oracle.** A call between a guard and an access ERASED the guard: every escaped cell was forgotten at every call, whether or not the callee could reach it. So `bump(var i); if i < 16 { bump(var j); a[i] }` was not proven — and the compiler was already emitting `restrict` on those parameters, asserting the non-aliasing to gcc while refusing to believe it itself. **That is the whole thesis of this project in one defect**: the ownership facts were being computed and then thrown away by the analysis that most needed them. Two IR-level footprints now answer the call: `ir_param_writes` (which parameters a call writes THROUGH) and the new `ir_param_retains` (whose ADDRESS may OUTLIVE the call), moved into `analysis/footprint.h` — below both the numeric domain and the effect lattice, because both want them and neither defines them. A call havocs exactly the cells that PERSIST plus the cells it was handed in positions the callee actually writes; an extern, an opaque, or an argument whose address cannot be attributed falls back to the blanket havoc, so `vra_arg_cell` returns THREE answers rather than two — getting that wrong is a removed bounds check, not imprecision. ★ **The soundness precondition was FALSE when I first stated it, and checking it is what found the rest.** A borrow CAN be stashed, so `persist` had to exist; asking HOW it is stashed found **two fail-opens that predated the oracle**: an address inside an AGGREGATE escaped nothing at all (`Holder(&i)` carries `&i` into an `IR_STRUCT_NEW` and the store that follows stores the STRUCT — the single-chain walk saw a constructor and gave up), and **D-15, in the SHIPPING compiler**: `type Holder { r var usize }` emitted a POINTER field and read it back WITHOUT a dereference, so `Holder(var x); h.r` printed an ADDRESS where the program asked for 7 — silent, no corpus test over it, now [E126] fail-closed until the borrow checker can carry a loan through a field. ★★ **TEETH, verified by breaking each half in turn** (0 false proofs whole, over 400 executed programs): write footprint ignored → **29 FALSE PROOFS**, persist havoc removed → **14**, aggregate escape walk removed → **14**. The third shape is new to the repo — `fire` is handed the Holder, never the cell, and its write footprint roots through a LOAD and names no parameter, so the write arrives ENTIRELY through the squirreled-away address. **And the payoff was measured before it was built elsewhere**: relaxing the remaining conservatism (parameter cells treated as persistent, which would need an interprocedural call-site fixpoint) is worth exactly **ZERO obligations on the corpus** — so it was not built. ★ Second item, same axis: **an UNSIGNED comparison proves NON-NEGATIVITY.** `(unsigned)i < n` is the C idiom for "valid index" and exactly what `i in arr` lowers to; the guard refinement read it as an ordinary `<` and took only the upper half, so the `in` ANNOTATION — the language's own precondition syntax — was being HALF-BELIEVED and `func get(arr i32[10], i i32 in arr) { arr[i] }` stayed unproven. Sound within the domain's existing model rather than by a new assumption. Teeth: a `signedidx` generator shape feeds negative indices through all four spellings and executes what is proven — dropping the signedness gate gives 4 false proofs reading `a[-1]`. Also: `run_tests.sh` learns `// LAINFLAGS:`, so a test can pin behaviour authoritative only under the sovereign engine, which is what the Stage 3.5 split MEANS; and `phase3_adjudications.txt` gains `new-catches-elsewhere` for a divergence that is an artifact of which pass the harness drives. corpus **650/650**, VRA **815/826** with **101 programs fully proven** (from 802/815, 99), reject survey REFUSED **34 → 36** with 0 PROVED-ALL — the rules strengthen the CATCHING direction too, --engine=ir **397 accepted / 0 false positives / 241 caught**, emit gate 326/22/24, trust 40/0, phase3 gate HOLDS, fuzz_{vra,loop,offset,bsearch,mask} all 0 unsound.
- **2026-09-07 (73)** — **★★ SOVEREIGNTY IS NOW TESTED, NOT ARGUED — a second front end exists.** The semantic-independence litmus ("could safe-C lower to this primitive without a Lain-shaped node?") has been an argument I re-ran by hand whenever the lattice grew; E0.6 asks for a demonstration. `src/frontends/cmin` is ~330 lines that parse a small C subset and build Lain-IR directly — own lexer, own parser, own symbol table — and it includes **only** `ir/*` and `analysis/*`, verified to reference none of ast.h/sema.h/parser.h. That is what makes it evidence: had it needed one of them, the BUILD would have said sovereignty was false rather than a document claiming otherwise. `cmin_gate.sh` runs it: the same analyses catch an unguarded index (E085), an uninitialised read (E005) and an unproven divisor (E087) in C, prove the guarded shape check-free, and a C program compiled THROUGH Lain-IR returns what gcc returns. ★ **The sharpest result is PARITY**: the same program written in Lain and in C produces the SAME TWO FINDINGS — including the same two LIMITATIONS (a symbolic-index fill is not provably total; an accumulator's overflow is not provably absent). The precision frontier lives in the IR, not in either front end. ★★ **And the cost was ZERO: nothing in the IR had to change.** No primitive was added, generalised or bent to accept a C front end; the type lattice, the instruction set and all five analyses were used exactly as they stood. That is the finding, and it is the strongest evidence for E0.2 that exists. Three sharp edges surfaced, none of them sovereignty problems and all recorded: source positions are a front-end OBLIGATION written down nowhere but `ir_emit` (cmin's first diagnostics had no line numbers); the analyses are wired through three globals a second consumer must know to set, one of which `borrow_analyze_mod` nulls on exit; and the subset does not exercise the ownership analyses at all, because C has no `mov` — so numeric and memory-safety sovereignty are DEMONSTRATED while the resource analyses remain ARGUED. Also this session, acting on the recommendation that Lain's own defects come before new syntax: a front-end DEFECT log (`lain_frontend_defects.md`, 13 entries) separate from the expressiveness limits, because the two were being conflated and the defects lived only in commit messages; and **D-2 fixed at the right layer** — sema typed every integer literal i32 while recording its exact value as a refinement, so `var x i64 = 5000000000` carried a refinement its own type cannot hold. Fixing it exposed a real bug in my own case-expression lowering: the no-`else` fall-through branched to the join without storing the result cell, and definite-assignment was right to call that an uninitialised read (10 false positives). Marked UNREACHABLE — a case expression must be exhaustive — rather than given an invented default, which would make an unreachable path look defined instead of saying it does not exist. corpus 637/637, --engine=ir 392 accepted / 0 false positives / 235 caught, emit gate 305/22/40, gate HOLDS 441, reject-side 0 PROVED-ALL, fuzz_trust + fuzz_ir_codegen 0, build warnings NONE.
- **2026-09-07 (72)** — **★ Stage IV measured, and the backend was not the problem.** Deleting `src/sema/` takes the OLD EMITTER with it, so the finish line is gated on whether `src/ir/emit_c.h` can compile the corpus. `emit_gate.sh` answers it by running the real corpus through both backends and diffing stdout + exit code — `fuzz_ir_codegen` only ever differentialled GENERATED programs, which are shaped like the bugs already known. Opening measurement: 209 agree, 15 differ, **141 could not build at all**. Now **293 / 34 / 40**. ★★ **Almost none of that gap was the new backend being immature.** It was seven front-end and lowering defects the old path papers over, and I would have mis-attributed every one of them to backend maturity without reading the emitted C: **string escapes were never DECODED** (`"ok\n"` stored as nine raw bytes, so `.len` was wrong for every literal containing an escape — the old emitter hides it by pasting raw text into a C literal and letting the C compiler decode); **externs lowered to a NAME and nothing else**, no return type, no parameters, no variadic flag, so no prototypes and every printf call an implicit declaration (102 of the 141, one cause); **`.data` on a slice had no lowering at all** — the idiom the entire C boundary is written in — and became an opaque unknown, printing "(null)"; **`*p = v` through a raw pointer was silently LOST**; **integer literals truncated to i32**, the exact bug the corpus test says the old engine already fixed; **error handling** — `T | m1 | m2` had NO type lowering, so it became its payload type and every marker vanished, leaving `try`/`else` nothing to test (the last of C3's three clusters, and the fix had to find sema's existing named enum rather than synthesise a second structurally-identical type, which is what the first attempt did and why a call's result stopped matching its callee's return); and **floats** — `IRT_FLOAT` and `aux.fimm` have been in `ir.h` since the beginning and **nothing ever produced one**, which is worse than an absent feature because it reads as supported. Three details the error-handling tests forced, each a real semantic point: the payload variant wraps its value in a one-field struct so the projection's type is that FIELD's; propagating a marker LEAVES THE FUNCTION and must run pending defers exactly as a return does; and `panic` is a declless builtin that nothing declared in the generated C, so every `else panic(...)` failed to LINK. corpus 637/637, faithfulness 612/614, gate HOLDS 441, --engine=ir 392 accepted / 0 false positives / 235 caught, reject-side 33 refused / 0 PROVED-ALL, fuzz_ir_codegen 100 @ 0 miscompile, build warnings NONE.
- **2026-09-06 (71)** — **★★ THE OWNERSHIP HALF OF 3.5 IS DONE — the sovereign analyses are authoritative in the REAL compiler, with ZERO false positives.** `--engine=ir` stands the legacy ownership checks down and lets the IR passes answer instead, on the actual user-facing path: real diagnostics, real exit codes, real messages. Over the corpus: **392 pass programs accepted, 0 rejected, 235 fail programs caught**, and every one of the 9 it does not catch was already adjudicated — five corpus corrections, two C-3 policy, two front-end spelling rules. That is the split, measured rather than argued: the analyses take over ownership, borrows and definite assignment; sema still resolves and types; the old backend still emits, because it has not yet earned the proofs. ★ **Getting there needed the IR to carry SOURCE POSITIONS**, which it did not: exactly ONE instruction in the whole lowering had a location, so every finding the new engine could produce pointed at line 0. Invisible for as long as the only consumers were differential harnesses that compare verdicts instead of reading messages — and a hard blocker, because an engine that cannot say WHERE is not one a user can run. ★★ **And building the gate priced the rest honestly.** `--engine=ir-full` also hands over the numeric obligations, and there it is 114 false positives — a number `phase3_differential.sh` structurally could not see, because it compares OWNERSHIP verdicts and would show a holding gate while the engine rejected two thirds of the corpus on overflow. Reading that number as "the new engine is over-strict" is wrong for a large share of it: the OLD engine rejects `a + b` on parameters and ACCEPTS `p.x + p.y` on struct fields — same expression, different operand source — and what it emits widens to int64 then narrows on return, so **`p.x = 2147483647; p.y = 1` prints −2147483648.** A compiler whose whole claim is prove-or-reject silently returns the wrong number; the new engine refusing that is old-unsound, not over-strict. **Precision landed alongside:** RELATIONAL range refinement (a usize's INT64_MAX bound is skipped by the entry seeding, so `while i < n` left `i` unbounded and `i = i + 1` — the commonest statement in the corpus — was unprovable; the octagon holds `i − n ≤ −1` and n is bounded by its TYPE, which together give `i ≤ typemax(n) − 1`), and **mutable-borrow parameters as numeric CELLS** — `var pos usize` is a pointer to caller storage that the domain modelled as nothing, so no guard on it ever survived; it is a stable cell because Lain's mutable borrows are EXCLUSIVE, the same guarantee the backend already relies on when it emits them `restrict`. ★ Checking that the guarantee is actually ENFORCED found a bug that had been silencing a whole check: `borrow_analyze_mod` sets `bor_loan_mod` on entry and NULLS IT ON EXIT, and the reporter passed that global back in — so the first function analysed got a module and every one after it got nothing, and the co-argument aliasing rule stopped running after the first callee, which is exactly where `mix(var x, var x)` lives. **Fail programs caught 219 → 235, missed 25 → 9.** corpus 637/637, vra 770/785, reject-side 33 refused / 0 PROVED-ALL, gate HOLDS 441, fuzz_vra + fuzz_linear 0, units ALL, build warnings NONE.
- **2026-09-06 (70)** — **★ 800x, and the false proof hiding underneath it.** The VRA took THIRTY SECONDS on one small program with a 128-element array literal — which nothing had noticed, because the corpus's programs are small and the new engine is not yet the one that runs. Part of it was my own measurement (the drivers build at -O0; -O2 alone is 6.4x). The rest was real and is now gone: an **active-set closure** (a ⊤ dimension can neither relax nor be relaxed), **variable packing** (rebuild item 2.2 — `dim` was 2 x EVERY SSA VALUE, element pointers and slices and structs included), and ★ **constants out of the domain entirely** — a constant's value is exact in the constant table, so it needs no dimension, and it was the single largest consumer of them. **4028ms → 5ms, and MORE PRECISE**, because an exact value beats any interval: the calculator program went 30 → 38 obligations and the tokenizer to 35/35. It required teaching every site that states or reads a relation to consult the table, and the one that mattered most was `vra_assign_copy` — without it `var i usize = 0` reached its slot carrying nothing at all. ★★ **Packing then exposed a latent UNSOUNDNESS**: `oct_widen_sel` reads its loop-modified table as `mod[i/2]`, a DBM index, while the table was built keyed by VALUE ID. Identical while the mapping was the identity; under packing they diverge, the widening consults an unrelated variable, and a loop counter that is never widened keeps a stale bound — the corpus's own soundness lock for `src[i]` under an unbounded `i < n` went to PROVEN. Caught by the reject-side survey, which is what it is for. **And nothing could have falsified it**: every fuzzer in the repo validates the OLD engine, so the new VRA's proofs stood on 33 corpus cases. **`fuzz_vra.sh`** closes that — generate a program whose safety rests on a new rule, ask the new engine, and if it says check-free EXECUTE it under ASan. Its first run found a MISCOMPILE rather than a false proof: `IRT_ARRAY` fell through to `void*` in the C emitter, so `a[i]` on a fixed-array PARAMETER advanced by BYTES under GCC's pointer-arithmetic extension — every fixed-array parameter in the new pipeline, invisible because `fuzz_ir_codegen` has no generator that passes an array to a function. ★ Making the fuzzer catch the WIDENING bug took two more attempts and is the lesson worth keeping: a wrong-keyed table is a **COLLISION** bug, so whether it misfires depends on which id lands on which slot — fixed-shape programs probe one arrangement and miss it entirely. With parameter order varied and dead locals padding the id space, reverting the fix makes it fire 2/200. **Precision, same session:** inferred CALL RETURN RANGES read off the callee's body (`LUT[nib(c)]`); module-constant folding, where sema mangles `<module>_<name>` with separators flattened while `defining_module` keeps the raw path, so NO module constant in any file had ever folded and every expression using one was blinded; division relational facts (`r ≤ x−1`, and `x − r ≤ xhi − xhi/D`, sound because `x − x/D` is nondecreasing); the **MIDPOINT** identity, which no octagon can prove — `mid < hi` needs a three-variable relation — DERIVED instead by substituting `d`'s definition (`d = B − A`) into the octagon's own `q − d ≤ c`, general in A, B, divisor and c; stack **VLA lengths**; and **struct `in` field invariants**, assert at construction and assume at the read, which needed four separate gaps closed first (a slice FIELD was not a cell; the same PLACE reached twice is two SSA values; the prepass order; and the bounds check discarding the index's TYPE — sound only for values that cannot have wrapped, as `predecessor_unguarded_fail` said within the minute). **754 → 766/785, 0 false proofs.** Recorded old-unsound: the old engine accepts a construction violating the invariant and emits `if (!(l->pos < l->text.len)) __builtin_unreachable();` — an unverified front-end annotation turned straight into a compiler assumption. corpus 637/637, gate HOLDS 441 agree, reject-side 33 refused / 0 PROVED-ALL, fuzz_vra 250 @ 0 false-proof, units ALL, build warnings NONE.
- **2026-09-06 (69)** — **★ A `chdir` in my own drivers had been hiding real bugs — the measurement was wrong before the analysis was.** Opening 3.2 I measured the ownership catch rate at 82/99 and started building against the 17 misses. Two of them were self-contained enough to reduce, and BOTH reductions passed — so the gap was not in the pass. Every analysis driver `chdir`s into the test file's directory; the real compiler does that only for ABSOLUTE paths, and the drivers had copied the code without the guard, so `import std.*` could not resolve and the harness counted the program as unmeasured. **Every downstream harness inherited it.** Fixed: gate **416 agree → 439** (skipped 210 → 183), VRA **601/634 → 749/785** — roughly 150 obligations that were never being measured at all. The 17 "gaps" were 15 already-caught plus 2 adjudicated. ★ **Restoring the measurement immediately exposed four FALSE POSITIVES it had been concealing**, and they were three real defects: **(1) ESCAPE = MOVE.** Consumption was only `mov`. An owned value read out of its slot and handed away has transferred ownership just as surely — so every std wrapper reported a leak on correct code (`open_file` returning `File(raw)`, `mem_free` freeing a cast). The callee's parameter mode is what makes the rule precise rather than syntactic. Move-on-assign is now the same rule, not a parallel one — it followed only `IR_LOAD` and so missed values that are resources in their own right. **(2) EXISTENCE.** A leak was reported at every return, including returns the resource never reaches — the early `return 1` of a function that acquires further down; a false positive on every function that validates before allocating. Created-liveness, MAY for E003 and MUST for E016. **(3) `ir_intern` DOES NOT INTERN** — it allocates a fresh `IrName` per call, so my callee lookup by pointer equality never matched and silently always took the permissive branch. **The actual 3.2 item, SUM PAYLOADS:** destructuring an owned sum moves the resource OUT of the scrutinee INTO the arm's payload binding, which carries the obligation on exactly the path where it exists. The corpus warns that discharging the scrutinee on a `case` is unsound — and it *was*, while the payload was untracked: the resource simply vanished, accepting both a leak and a double free. With the payload tracked the transfer is complete rather than a hole. Not argued — each of five shapes was RUN: leak → E003 on the payload, double free → E001, never destructured → E003 on the scrutinee, destructured twice → E001, correct release → clean (CORPUS_CORRECTIONS C-5; the test's own header authorises the inversion). The Empty arm reports nothing because the payload is projected only on the Full block — **per-variant reasoning falls out of the CFG**, not from a special case. Also fixed: **destructuring parameters were dropped ENTIRELY by lowering** — `close_file(mov {handle} File)` lowered with NO parameters while its call sites still passed an argument, so the IR disagreed with itself on arity and ownership analysis had to guess. corpus 637/637, gate HOLDS (**439 agree**, 0 unadjudicated, 0 unsound, 0 effect drops), linearity units 29/29, IR faithfulness 534/536 (99%), reject-side 0 PROVED-ALL, fuzz_linear/ir_codegen/combo all 0, build warnings NONE.
- **2026-09-06 (68)** — **Made the gate's UNMEASURED bucket auditable — and it corrected me twice.** ★ First I had to withdraw a claim from (67): I wrote that the harness "reports AGREEMENT when it cannot run one side" and built a general lesson on it. **False** — `phase3_differential.sh` has a `skipped` bucket and had those cases in it (measured: suppressing E087 moved 3 tests skipped 213 → 210, agree 413 → 416; they were never counted as wins). I asserted a mechanism instead of reading the script. What IS true is weaker: the bucket CONFLATES "out of scope" with "could not look" under a label — `skipped (non-ownership)` — that asserts the conclusion, and at 210 against 416 compared that is a third of the corpus declared irrelevant without evidence. The gate now reports WHICH DIAGNOSTIC stopped the old engine, per code: E086 47, E085 39, E012 29, module-load 28, E082 9, E011 9, E100 8, E087 5. ★★ **The breakdown then corrected me a second time within the hour.** Seeing `[E087] 5` I assumed a residual hole and extended the seam to the other E087 sites — 4 tests became "measurable" and all 4 were NEW-ACCEPTS purely because a sized-slice LENGTH precondition is not an ownership question. Reverted; inspecting the 5 shows every one is the length family, not aliasing. **The sharpened rule: a skip is a hole only when the blocking diagnostic is in the gate's OWN subject area** — E087-aliasing was (it hid two real gaps), E087-length is not, and E085/E086/E012 are measured by vra_reject_survey.sh instead. A per-code breakdown is what makes that judgement possible; a total cannot express it. corpus 637/637, gate HOLDS (416 agree, 210 unmeasured, 0 unadjudicated), IR units ALL, build warnings NONE.
- **2026-09-06 (67)** — **The litmus I skipped, then B4 and C5 — and the harness itself was hiding two gaps.** Ran the semantic-independence re-audit E0.2 demands "whenever the lattice grows": it had grown by ELEVEN ops and I had not re-run it. **Result: sovereignty INTACT** — every new primitive is a concept safe-C/safe-Fortran/safe-Rust already have, and `IR_SHAPE` is literally Fortran's native array model, the "generalize upward" outcome the doc asks for. But the re-run surfaced what the per-op checks could not: **three PLACE FACTS in six sessions, each locally right, all living beside the type lattice** — C6's `slicelen` complaint tripled (recorded as §8). **B4** acts on it: `IrType.has_refine/lo/hi`, read by `irtype_int_range` so bounds, overflow and div-by-zero all tighten from ONE field. `type Small = u8 < 200` was being DISCARDED when the alias resolved to its base, so `n Small` was indistinguishable from `n u8` — while the SAME constraint on a parameter proved, because that path goes through the assumes. ★ The scope rule that makes this right: a static interval is the same for every value of the type and belongs ON it; a slice's length and a region's extents are per-VALUE and belong in the IR. B4 is not "move everything onto types". **C5** adds the write FOOTPRINT (`IrFunc.param_writes`, transitive, conservative for extern/opaque) and the conflict rule now takes mutability from BEHAVIOUR, not the parameter type — which was wrong in both directions: a `var` scalar the callee never writes is not a conflict (restrict is violated by a WRITE), and a SLICE/STRUCT param never set is_mut at all, so `vadd(n,a,a)` went uncaught. ★★ **That second gap was invisible because of the HARNESS.** `g_suppress_ownership` did not cover E087, so every aliasing program stopped in the old typechecker before the new pass ran — twice I read "accepted" from a program that had never been analysed, once writing it up as an unsoundness before catching the artifact. Extending the seam immediately exposed TWO REAL GAPS. **CORRECTED (68):** I first wrote that they "had been counted as AGREEING" and generalised that into a lesson — FALSE. The gate has a `skipped` bucket and had them in it (suppressing E087 moved 3 tests skipped 213 → 210, agree 413 → 416). I asserted a mechanism instead of reading the script. **416 agree** (413 → 416), 2 divergences left, both the C-3 policy question. corpus 637/637, vra 601/634 with 0 false proofs, fuzz_borrow/linear/alias/combo/ir_codegen all 0.
- **2026-09-06 (66)** — **The BORROW divergence column is EMPTY; 2 divergences left and both are a POLICY question, not a bug.** `case &x { 42: x = 99 }` borrows the scrutinee for the whole match and NOTHING IN THE ARMS READS THAT BORROW — so a liveness-derived region is empty and the write looked legal. Most loans come from liveness; some are scoped by a CONSTRUCT, and no value's liveness expresses those. IR_BORROW/IR_BORROW_END state the extent (no runtime effect, like IR_SHAPE and IR_INIT; language-neutral — Rust borrow regions, C++ reference lifetimes, Fortran ASSOCIATE). ★ **The region is a CFG REACHABILITY set, not a textual span, and getting that wrong made the first attempt silently do nothing:** a linear scan over the flattened instruction list hit `borrow_end` immediately, because the match's JOIN block is emitted BEFORE the arms — the region closed before reaching the writes it exists to catch. Four unit cases pin it, including a write in a BRANCH the region reaches. (A second slip the unit tests caught at once: I copied the other phases' `if (mod)` guard, but a write conflicting with a scoped borrow is visible inside ONE function.) **Board: 413 agree, 3 wins, 5 front-end-policy, 2 gaps, 0 unsound, 0 unadjudicated** — from 25 gaps when this run began. The 2 remaining are C-3: whether an unconsumed owned value with NO releasable resource is an error. That is Lain deciding its own semantics — the old engine currently answers it INCONSISTENTLY (borrow_pass accepts, rich_diagnostics rejects, same shape) — so it is a Stage V language decision, not something to fix unilaterally. corpus 637/637, IR units ALL (21 borrow cases), fuzz_borrow/fuzz_linear 300 each @ 0 unsound and 0 over-rejected, build warnings NONE.
- **2026-09-06 (65)** — **Definite-assignment CLOSED: per-element arrays fixed a fail-open and an over-rejection at once.** The last two divergences in that column, re-judged — and NEITHER was what the ledger claimed. The new engine MISSED `var a i32[4]; return a[0]` (reading storage nothing ever wrote) because arrays were untracked; the old engine REJECTS `a[0] = 5; return a[0]`, which is safe, and its own diagnostic gives the reason — "initialize it with a whole-array value before reading elements", a statement about the checker rather than the program. Tracking elements at constant indices fixes both directions. ★ Enabling it immediately produced 6 false positives, all comprehensions: `[expr for i in 0..N]` lowers to a fill LOOP and a MUST-analysis intersects the fact away on the zero-iteration path. **IR_INIT** states what lowering already knows — a comprehension fills every element by definition — with no runtime effect, like IR_SHAPE. Making the analysis re-derive it would need a proof that the loop covers 0..len. Both tests were also MISFILED: one is `new-more-precise` (CORPUS_CORRECTIONS C-4, inverts at 3.5), the other is [E100] "a comprehension is only supported as a variable initializer ... FOR NOW" — an explicit parser limitation, never a dataflow question. **Remaining genuine gaps: 3** (1 borrow, 2 linearity policy), from 25 at the start of this run; 412 agree, 3 wins, 5 front-end-policy. Still fail-open and stated plainly: a store at an UNKNOWN index marks the whole array initialised, so a half-filling loop is not caught. corpus 637/637, IR units ALL, fuzz_ir_codegen 0, build warnings NONE.
- **2026-09-06 (64)** — **★ PHASE D: the beyond-Rust seam is filled — the octagon answers `a[i]` vs `a[j]`.** Rust's borrow checker cannot distinguish them at all (both are the place `a[_]`), which is why `split_at_mut` needs `unsafe` and a justifying comment; here it is a QUERY. **The interface was the real work:** the octagon state is PER-BLOCK and `ir_place_index_disjoint_fn` took only two values, so a query had no point to be asked at. Answering from the block ENTRY misses everything established inside it; answering from the converged whole-function fixpoint would be UNSOUND, since a fact true at one point is not true at another. The context now carries block+instruction and REPLAYS `in[block]` up to the query. **Direction matters and is pinned:** the default stays conservative and the bridge may only ever REMOVE a conflict it can prove away — five unit cases cover both sides, including `a[3]` vs `a[3]` through two different SSA values (must still conflict) and ★ the RELATIONAL one, `a[p]` vs `a[q]` under `p<q`, where neither index has a value and the octagon proves they differ anyway. Closes the `new-over-strict` divergence that motivated it (411 → 412 agree). Two honest caveats: it is wired into the CO-ARGUMENT rule only, so a cross-statement `a[i]`/`a[j]` pair is still conservatively rejected; and one scare during the work was a MEASUREMENT ARTIFACT of my own making — the E087 fix from session 63 now fires first, so the "unsound" reading came from a program that never reached the new pass. Verified: corpus 637/637, gate HOLDS (412 agree), IR units ALL (17 borrow cases), fuzz_linear 300 @ 0 unsound/0 over-rejected, fuzz_borrow 300 @ 0 unsound/0 missed-dangles, build warnings NONE.
- **2026-09-06 (63)** — **Phase-D groundwork found a DEMONSTRATED MISCOMPILE in the shipping compiler.** Setting out to fill the `a[i]` vs `a[j]` disjointness seam, the first question was what the two engines currently do with element borrows. The NEW engine rejects both `swap2(var a[i], var a[i])` and the disjoint `swap2(var a[i], var a[j])` — conservative, sound, over-strict. The OLD one accepts BOTH: `mix(var a[i], var a[i])` passes one pointer to two `int32_t * restrict` parameters and **-O0 gives 226 while -O1/-O2/-O3 give 116**. Two independent causes, either of which alone would have hidden it: the check only considered array/slice/pointer params (a mutable SCALAR param is emitted `restrict` too), and `sema_expr_root_id` never unwrapped the borrow wrapper, so `var a[i]` reported NO ROOT — silently skipping the check for exactly the arguments that carry the hazard. Fixing either one alone still accepted the program, which is a fair warning about declaring this class closed from one repro. The disjointness exemption grew with it (`a[k]` is the span [k,k+1), folded through the RANGE TABLE rather than requiring a literal) so correct code keeps compiling; both directions locked by a _fail/_pass pair. ★ The new engine's rejection of the DISJOINT case is now adjudicated `new-over-strict` — the first entry in that column, and the concrete motivation for phase D: fill `ir_place_index_disjoint_fn` from the octagon. **Rust cannot distinguish a[i] from a[j] at all** (why `split_at_mut` needs `unsafe`), so that seam is the project's most defensible beyond-Rust claim. What it still needs is a query POINT: the octagon state is per-block and the hook takes only the two values. corpus 635 → 637, gate HOLDS (411 agree), fuzz_alias 250 @ 0 ALIAS-MISCOMPILE, fuzz_trust/fuzz_combo 0.
- **2026-09-06 (62)** — **STAGE II unblocked: B3 (totality) and S2 (memory model) both land, and S2 CONFIRMS the retrospective's sharpest finding.** Went back to finish the stage we had been running ahead of. **B3 — `IR_OPAQUE`:** an unmodelled construct is now a declared unknown with a footprint instead of a bit that poisons its whole function. `incomplete` **60 → 13**, faithfulness **88% → 97%**, and precision went UP (576/612 → 587/622) because more code became reachable. ★ The gate earned its keep twice here: with suppression gone the effects pass began analysing 4 functions it used to skip and an opaque contributing no effects made them look PURE — the row that gates `const`/`pure` C annotations, where a wrong answer is a miscompile; and the first fix (IO|WRITE) was STILL not conservative enough, since `else panic` inside an unmodelled expression lost its Raises. **S2 — `IR_SHAPE`:** `a[i*w + j]` over `i32[h*w]` is a NONLINEAR obligation flat (`i*w + j < h*w` multiplies two runtime values) and no octagon can ever express it; with the region's extents stated in the IR it FACTORS into `i < h ∧ j < w`, both facts the loop guards already established. The intermediates' overflow falls out of the same fact (operand intervals cannot see it: i and w are each unbounded, so their product's interval is the whole type). matrix_symbolic_2d **15/21 → 18/21**. **The 2D case was a memory-model gap, never a numeric one — exactly as the retrospective said, after sessions spent on incremental bounds precision instead.** Negative cases unit-pinned: widen either guard by one and factoring refuses. ★ **Two soundness items surfaced on the way, one of them a FALSE PROOF:** the octagon modelled a scalar alloca as a stable cell that NOTHING invalidated, so `bump(var i)` setting i=100 left `a[i]` on a 4-element array "PROVEN check-free" — a removed bounds check; escaped cells are now havoced at a call (precision cost: zero). And a bare `break` I had added ran off the end of `ir_lower_expr`, returning a garbage pointer the VRA dereferenced — I first called it pre-existing and bisecting proved it mine. ★ gcc HAD warned; I grep build output for `error` and discard warnings. `check_build_warnings.sh` now fails on the never-benign class and immediately found 8 more, including one of my own tests with `ir_const_int`'s VALUE and TYPE swapped (it passed anyway). Verified throughout: corpus 635/635, gate HOLDS (411 agree, 0 effect drops), IR units 6 suites, reject-side 0 PROVED-ALL, fuzz_vra SOUND, fuzz_2d/fuzz_ir_codegen/fuzz_combo all 0.
- **2026-09-06 (61)** — **★ B3: the IR is now TOTAL (`incomplete` 60 → 13, faithfulness 88% → 97%) — and getting there surfaced a FALSE PROOF.** Went back to finish STAGE II, which we had been running ahead of: three sessions of Stage-III analysis work were stacked on an incomplete IR core. **(1) A REAL UNSOUNDNESS, found while surveying what `incomplete` protects.** The octagon models a scalar alloca as a stable memory cell and NOTHING invalidated it when someone else could write: `bump(var i)` setting i=100 left the analysis believing i==0, so `a[i]` on a 4-element array was **PROVEN check-free** — a removed bounds check. An alloca's address ESCAPES when passed to a call, stored as a value, or returned (provenance followed through the address-forming ops); every escaped cell is now forgotten at a call. **Precision cost: none** — 576/612 before and after. This had to land FIRST, because without it "havoc memory" has no meaning in this domain. **(2) A SEGFAULT THAT WAS MINE, and the process failure behind it.** A bare `break` in the SIMD-builtin path ran off the end of `ir_lower_expr` — UB returning a garbage IrValue* the VRA dereferenced, crashing on every @movemask/@load program. I first reported it as pre-existing; bisecting showed rc=0 before my own bit-intrinsics commit. ★ gcc HAD WARNED (`-Wreturn-type`) and I never saw it, because I grep build output for `error` and discard warnings — that is the actual defect. `check_build_warnings.sh` now fails on the correctness class only (never-benign: return-type, uninitialized, implicit-decl, int-conversion, incompatible-pointer). It immediately found 8 more including one of MY OWN tests passing `ir_const_int(f,blk,TYPE,VALUE)` with the arguments swapped — the test passed anyway, which is the point. The gate had the same silent-skip flaw it exists to prevent (grepping only for warnings made compile ERRORS invisible); fixed while writing it. **(3) B3 proper.** `IR_OPAQUE` + a declared footprint replaces "poison the whole function because one expression is unlowered". Each consumer learned the footprint, and ★ **the gate earned its keep**: with `incomplete` gone the effects pass began analysing 4 functions it used to skip, and an opaque contributing no effects made them look PURE — the row that gates `const`/`pure` C annotations, where a wrong answer is a miscompile. The first fix (IO|WRITE) was STILL not conservative enough; `else panic` inside an unmodelled expression lost its Raises. An unmodelled construct may do ANY observable thing. **Precision went UP** (576/612 → 587/622): more code reachable AND a higher proven rate. `incomplete` now means only unmodelled CONTROL FLOW (2 functions); 11 more conversions remain. Verified: corpus 635/635, gate HOLDS (411 agree, 0 effect drops), IR units 6 suites, reject-side 0 PROVED-ALL, fuzz_ir_codegen 250 @ 0, fuzz_combo 120 @ 0, fuzz_vra SOUND, build warnings NONE.
- **2026-09-06 (60)** — **fuzz_combo: the first NON-VERTICAL fuzzer, because the corpus is the rebuild's oracle and it has a systematic hole.** All 15 existing fuzzers drill ONE feature; none cross two — and every bug found by hand in session 59 lived at an intersection. The argument for building this is not the bug count: every "gate holds — 411 agree" claim is measured against the corpus, and two of session 59's fixes were things the corpus positively ASSERTED were correct. `fuzz_combo.py` composes SHAPE (struct / ADT / ADT-with-struct-payload / nested ADT / nested struct / array-of / slice-of) × CARRIER (local / shared / var / mov param / call result / array element) × OP (read / write / whole-assign / match / pass-on / defer); every program is valid by construction and SELF-CHECKING, which yields three verdicts with no second pipeline — WRONG-REJECT, BROKEN-C, MISCOMPILE. **Teeth verified on all four paths** (reverting the match-on-by-ref-param fix → 38 BROKEN-C; the Fixed-wrapper indexing fix → 4; the typed-payload-binding fix → 14 WRONG-REJECT; perturbing the expected value → 30/30 MISCOMPILE). **600 programs clean** against the current compiler. ★ Three harness lessons, all found while building it and all recorded in memory: a generator crash was being SILENTLY SKIPPED (`|| continue`), so a run could report "all clean" over programs never produced; adding an axis to the list is NOT adding coverage (`mov_param`, `sliceof`, `defer` were selected for a while before being implemented, falling through to unrelated programs — worse than absent, since the label claimed coverage that did not exist); and `%%` inside a `cat <<EOF` heredoc emits a literal `%%`, which had left two of session 59's generators largely INERT. Then probed **error unions** (the next lowering item) and found the surface is much narrower than assumed: E064 REJECTS any union whose payload has no niche (`i32 | NotFound`, `P | NotFound`) — a deliberate zero-cost-or-nothing rule with a good diagnostic, not a bug. But `var r = f()` cannot INFER a union type: `T | markers` is lowered to a synthesized enum by union_lower() only for a written-out type, so the variable adopts an unnamed placeholder and fails its own conversion check with "cannot convert '__U_ptr_u8_NotFound' to '?'". Every corpus union test annotates explicitly — the same one-way-only coverage again. ★ **Tried to make it infer and STOPPED**: the fix must route the inferred type through union_lower AND follow a raw TYPE_UNION into the emitter, i.e. a multi-layer change to the NICHE path, which is what guarantees these unions cost nothing. Reverted; shipped the honest half — a diagnostic that names the limitation and gives the fix, plus a _fail/_pass PAIR documenting limitation and workaround together, with the attempt described so the next one starts informed. **corpus 633 → 635, gate HOLDS (410 → 411 agree), fuzz_combo 600 @ 0, fuzz_trust 300 @ 0.**
- **2026-09-06 (59)** — **SUM TYPES land (faithfulness 80% → 88%), and probing COMBINATIONS found SIX bugs in the SHIPPING compiler.** Two halves. **(A) The measured lowering gap.** `ir_incomplete_survey.sh` ranked enums at ~42/103 incomplete functions, so that is what got built: `IRT_SUM` + `IR_SUM_NEW`/`IR_SUM_TAG`/`IR_SUM_PAYLOAD`, all three construction forms, and `case` with multi-field/single/payload-less arms. ★ The decision that matters is what the IR does NOT record: the old backend niche-packs an error union (`*u8|NotFound|Denied` is literally a pointer with markers at 0 and 8), and modelling that would make a sum indistinguishable from a pointer with an odd range — destroying the discrimination every analysis needs. LAYOUT stays in the backend. Because the tag is an ordinary integer, `case` lowers to the same if-chain as an integer match. `enum-match` 14→0, `unresolved-member` 37→4. Then the next-ranked bucket, measured the same way: 11 of 22 `unhandled-expr` builtins are the SCALAR bit intrinsics, so IR_CTZ/IR_CLZ/IR_POPCOUNT became real ops — which hands the numeric domain `@popcount(u32 x) ∈ [0,32]` for free, making `a[@popcount(mask)]` provable check-free (4 VRA tests pin the proof AND its refusal when the range escapes). **(B) ★ The richer vein: the corpus tests each axis alone and never CROSSES them.** Writing one self-checking ADT test showed the shipping backend emitting uncompilable C for `case` on a struct PARAMETER — entirely ordinary code — because a borrowed aggregate arrives by address while `__matchN` is bound by value. Deliberately probing the combination space (aggregate kind × how it is passed × what is done with it) then found five more: whole-value assignment through a `var` aggregate param; `case o.tag` on a struct field falsely reported non-exhaustive; match-arm payload bindings that were never DECLARED and so had no type (`Pt(p): p.x` → E102); the `Fixed_<UserType>_N` typedef emitted after the prototype that names it; and indexing the Fixed WRAPPER instead of `->data[i]` through a pointer. Six regression tests added, each recording why the corpus missed it. **(C) And two miscompiles in the NEW pipeline**, found by diffing probe programs against the old one: a field read off a value with no address (`mk(i).x`) fell back to an UNINITIALISED placeholder slot, so the call never happened and the program silently computed nonsense; and struct bodies were emitted in reverse-discovery order, which is not a dependency order (now a real topological emit over by-value containment). **★ Also one UNSOUND change tried and REVERTED**: "a non-borrowed `case` consumes its scrutinee" looks right and discharges the obligation without anyone releasing the resource — it accepts both a double free and a leak. Caught by checking those two cases before trusting it; the reasoning is left at the site and in the test so it is not re-attempted. Verified throughout: corpus 626→633, gate HOLDS (403→410 agree), IR units ALL, fuzz_ir_codegen 250 @ 0 miscompile + 0 broken-C, fuzz_trust 300 @ 0, fuzz_linear 300 @ 0. ★ Two fuzzer generators were teeth-confirmed BEFORE being trusted (deliberately break the tag / revert the fix), and that discipline caught a third problem: `%%` inside a `cat <<EOF` heredoc emits a literal `%%`, which had left the string and defer generators added last session largely INERT (old-rejected 19→1 once fixed).
- **2026-09-05 (58)** — **Divergences 25 → 5, and `incomplete` finally has a WORK LIST.** Continuation of (57), same discipline: judge each remaining divergence on its merits, and where the new engine loses, ask whether the missing thing is an ANALYSIS gap or something more basic. It was usually the latter. **Closed this half:** per-field linear state (a consumption MASK per slot over the place lattice — plus a LOWERING gap, `mov r.handle` emitted no IR_CONSUME at all, so consuming a field was invisible) and failing CLOSED on an opaque linear struct (concluding "owns nothing" about a type we cannot SEE had silently exempted every std handle from leak checking); a MOVE as an ACCESS (a consuming parameter invalidates the place, so it conflicts like a mutable borrow — design §2, one conflict rule); a second MUST-moved lattice beside the MAY one (same transfer function, intersection merge — MAY ∧ ¬MUST is exactly E016 "consumed on some branches but not others", invisible while the pass asked only ¬MAY); **`defer` lowering** — never an analysis gap at all, `defer` hit the fail-closed default and the whole function was skipped, so nobody was looking; depth-2 definite-init, which was a **FAIL-OPEN** rather than imprecision (`p.a.y = 1` marked all of `p.a` initialised, so reading the unset `p.a.x` reported nothing). **★ A second silent miscompile caught before it shipped:** Lain runs `defer` bodies BEFORE evaluating the return expression, so a defer can change what is returned (`defer acc=acc+1; defer acc=acc*10; acc=5; return acc` is **51, not 5** — Go would give 5). The obvious reading returned 5; verified against the old backend's emitted C and now guarded by a gen_defer generator. **Re-classified 3 more as front-end-policy** after judging them: E106 undeclared identifiers (the IR has NO NAMES — a value is an SSA id with a defining instruction, so a well-formed IR cannot represent an undeclared reference and no IR analysis can be written that catches it) and E017 call-site `var` spelling (the dual of E007). **★ C3 CLOSED as a measurement:** every `incomplete` site now names its reason and `ir_incomplete_survey.sh` ranks them — **432/535 functions (80%) lower faithfully**, and the ranking overturned my assumption about the top entry: `unresolved-member` is dominated not by struct fields but by ENUM VARIANT references, so **enums are ~42 of 103 (41%) of all remaining incompleteness** — the largest single lowering gap and the obvious next target (tracked below). **Session total: 25 gaps → 5** (1 borrow, 2 linearity, 2 definite-assignment), **verdicts agree 388 → 402**, 2 adjudicated wins, 4 front-end-policy, **zero false positives at every single step**. Verified: corpus 625/625, gate HOLDS, IR units ALL (5 suites — test_definite_init is new; the pass had none), fuzz_ir_codegen 250 @ 0 miscompile, fuzz_linear 300 @ 0 unsound.
- **2026-09-05 (57)** — **★ THE CORPUS IS NOT GROUND TRUTH — and doubting it found bugs in the CHECKER.** Acting on "the frontend may not be a perfect language and so the tests", I re-judged all 25 tracked divergences on their merits instead of assuming the old engine was right. I had classified every one of them `new-incomplete-*` — the exact bias the C1 gate's `old-unsound` / `new-more-precise` columns exist to expose. Findings, in the order they cascaded: **(1) TWO CORPUS ERRORS.** `mut_plus_nested_shared_fail` asserts `scale(var v, length(v))` is "an aliasing violation" — it is `v.push(v.len())`, the canonical two-phase borrow, and the corpus CONTRADICTS ITSELF by requiring the same shape to PASS in `two_phase_borrow_pass`; the only difference is that the shared read goes through a call, the old engine's documented blind spot. `multi_var_param_fail` asserts db "is still borrowed" after `pick_x(var da, var db)` returns `var a.x` — false; only the returned reference's loan outlives the call. Both are `new-more-precise`, the first entries in that column, and both INVERT at 3.5 — recorded with their evidence in the new **`CORPUS_CORRECTIONS.md`**, which also fixes the standard of proof for putting a test there. **(2) A REALto expose. Findings, in the order they cascaded:  UNSOUNDNESS IN MY OWN ENGINE**, visible only from inside (1): judging `multi_var_param` needs to know WHICH parameter a return borrows from, and I was GUESSING it from the signature ("first mutable reference param") — so `return var b.x` charged the loan to `a` and lost every conflict on `b`. Now INFERRED from the body (`bor_ret_borrow_mask`), which is strictly stronger than Rust: rustc has no whole-program view and rejects the ambiguous signature outright. **(3) A LIVE SEGFAULT in the new backend**, found by taking `struct_field_dangling_fail` seriously: the two backends disagree about where a string literal's bytes LIVE, and both models are wrong — old emits a block-scope compound literal (writable but DANGLES on escape), new emits a C string literal (static but the first write FAULTS). `fuzz_ir_codegen` had no string generator at all; added one, confirmed teeth (**8 MISCOMPILES**), fixed lowering so a MUTABLE string binding gets its own storage, back to **0/250**. **(4) THE OLD ENGINE IS INCONSISTENT** about unconsumed owned values (`borrow_pass` must accept, `rich_diagnostics_fail` must reject — same shape; the discriminator is how the local was INITIALISED, not the value). Also built this session: **move-on-assign** (`var q = p` on a linear value TRANSFERS it — the invisible `free(p); free(q)` double free, closes 3), **`IrValue.owns`** (ownership is a property of the BINDING, not the type — Rust `T` vs `&T`; the missing concept behind 4 false positives, and its release-obligation rule catches a struct-owning-a-heap-pointer leak the old narrowing MISSED), **generalised loan sites** (the conflict rule is over ACCESSES not calls — direct stores and direct address-of, closes 3), **escape through a load** from a singly-stored slot (closes 1), and a **`front-end-policy`** classification for rules that are correctly NOT the IR's job. **Divergences 25 → 16 gaps + 2 wins + 1 policy; verdicts agree 388 → 395; zero false positives at every step.** Verified: corpus 625/625, gate HOLDS, IR units ALL (12 borrow, 16 linearity, place, VRA), fuzz_ir_codegen 250 @ 0 miscompile, fuzz_linear 300 @ 0 unsound.
- **2026-08-29 (56)** — **Borrow phase A2: liveness regions (5 more closed; tracked gap 42 → 25).** The cross-statement family (`r = get_ref(var d); read_data(d); use(r)`) needed a loan to outlive its statement. Two pieces: **(1) B5 region info on the IR** — Lain has no lifetime syntax, so "the return borrows from a parameter" was INVISIBLE (a `var i32` return lowers to a plain i32, making `r = get_ref(var d)` look like an ordinary value). Added `IrFunc.{ret_borrows,ret_borrow_param}`, filled at lowering by Rust-style **ELISION** (design §5: a function returning a reference borrows from its reference parameter, preferring the sole mutable one) — exactly the substrate the design predicted we'd need before lifetimes exist. **(2) Liveness regions** (design §1.3): the loan lives until the carrier's LAST USE (the result value plus the slot it is stored into); a conflicting borrow of an overlapping place inside that window is an error. Liveness bug worth recording: I first counted only LOADs of the carrier slot, but `consume_ref(var ref)` passes the slot's ADDRESS — which is exactly how these tests keep the borrow alive; counting any operand mention caught 5 tests at once. **borrow 14 → 9, verdicts agree 383 → 388, ZERO false positives, GATE HOLDS.** Now caught: nll_still_active, cross_stmt_borrow, reborrow, reborrow_same_stmt, nll_loop_borrow. **Session cumulative: tracked gap 42 → 25** (9 borrow, 9 linearity, 7 definite-assignment), verdicts agree 371 → 388, every step 0-FP. Verified: corpus 625, gate 15/15, IR unit tests, reject 0 PROVED-ALL, fuzz_ir_codegen 0 miscompile.
- **2026-08-29 (55)** — **Borrow phase A: loans + the conflict rule (4 more closed; tracked gap 42 → 30).** First real NLL piece on the place lattice: each call argument naming storage creates a LOAN, and two loans on OVERLAPPING places conflict when at least one is mutable (design §1.4) — specialised to co-arguments so it needs no region inference. **The gate forced the load-bearing insight:** a loan is created only by a BORROW, never by a by-value COPY. A scalar argument is read during argument evaluation and cannot conflict; an aggregate parameter (ptr/struct/slice) is a live reference for the call. My first cut treated any place-naming argument as a loan and produced 2 false positives — `ms(var a, a)` and `v.push_n(v.cap)`. ★ Those are precisely the TWO-PHASE BORROW pattern, and the copy-vs-borrow rule accepts them with NO two-phase machinery (a copy is not a loan) — a capability the old engine only gets by special case and still fails for nested-call args (E004). By-value shared borrows stay visible because a LOAD argument yields a loan on the place it was loaded FROM, which is what keeps `f(data, var data)` detectable. **borrow 18 → 14, verdicts agree 379 → 383, 0 FPs, GATE HOLDS.** The remaining 14 all need LIVENESS REGIONS (a loan live across statements): cross_stmt_borrow, reborrow, nll_*, block_scope, elif_borrow, … — phase A2. Verified: corpus 625, gate 15/15, IR unit tests, reject 0 PROVED-ALL, fuzz_ir_codegen 0 miscompile.
- **2026-08-29 (54)** — **Stage II places + Stage III definite-assignment — 8 divergences closed, gate holds.** **B5-real PLACES** (ae8a859): `ir/place.h` gives rooted access paths (base + .field/[index] projections) and `ir_place_overlaps` — the one relation the borrow conflict rule AND definite-assignment both reduce to, so those stop being bespoke pointer-chasing passes. Distinct roots and distinct fields are provably disjoint; indices are delegated to `ir_place_index_disjoint_fn`, the ★ seam the VRA fills at Stage III-D (Rust collapses a[i] and a[j] into one place — this is where we beat it). test_place.c 12/12 verifies BOTH sides of that seam. **3.0 DEFINITE ASSIGNMENT** (aa49b0c) built on it — the analysis the plan had missed: forward MUST-init dataflow, merge = intersection, E005/E019. **Divergences 15 → 7, verdicts agree 371 → 379, 0 false positives, GATE HOLDS.** The gate caught TWO false-positive waves and forced real fixes: treating every operand as a read (but `a[i]=v` passes the alloca as an ADDRESS — a read is exactly an IR_LOAD, ~18 FPs), and tracking aggregates (a MUST-analysis can't prove a fill loop runs, so every comprehension-filled array looked uninitialised — 8 FPs; arrays AND slices/VLAs now untracked). Remaining 7 sub-classified honestly: undeclared identifiers (resolve-level), per-element array init (needs the VRA fill-loop proof), nested field paths (needs a path trie). Verified: corpus 625, gate 15/15, IR unit tests pass, reject 0 PROVED-ALL, fuzz_ir_codegen 0 miscompile.
- **2026-08-29 (53)** — **★★ COMPLETE ENDGAME PLAN + STAGE I DONE (the unblock).** Audited the plan before committing to a long build and found it **incomplete**: detailed through Phase 3, but Phase 4 was three bullets with nothing beyond, the front-end rework / multi-language frontends / formal spine were unplanned, "done" was undefined, and TWO roadmaps with clashing Phase 0–4 numbering made navigation hazardous. Added **"★★ THE COMPLETE ENDGAME"** (authoritative for sequencing): **E0** nine concrete acceptance conditions for "done"; **E1** invariants holding at every step; **E2** dependency-correct Stages I–VII (Unblock → IR core to full power → analyses to maximal power → backend earns the proofs → the LANGUAGE catches up → prove sovereignty with a SECOND FRONT-END → formal spine); **E3** a navigation table. `GRAIL_PLAN.md` marked superseded for sequencing. **STAGE I COMPLETE:** **C1** (3654003) the parity criterion — a *structural mistake* that forbade the new engine from ever being stronger — is replaced by **adjudicated divergence**: 371 agree, 211 skipped, and **42 divergences now CLASSIFIED and TRACKED** (18 borrow, 9 linearity, 15 definite-assignment), 0 unadjudicated, 0 new-unsound. ★ Classifying them exposed **a whole analysis the plan had MISSED — DEFINITE ASSIGNMENT** (the old linearity.h quietly implements E005/E019, so "port linearity" silently meant two analyses); added as Stage III item 3.0. **C2** (331ecb5) `fuzz_ir_codegen.sh` — the first execution-differential fuzzer for the NEW pipeline (all 14 others test the old one; that blind spot hid the scalar-`var` miscompile). **Teeth-verified**: reverting the fix makes it fire 4/40; with the fix, 200/200 agree, 0 miscompile, 0 broken-C. **C3** (e4c0eb5) `ir_coverage.sh` — the `incomplete` denominator every metric was silently conditioned on: **82% (533/648 functions)**, and the gap is concentrated in error handling, enums/match/niche and defer → feeds B3 directly. The endgame is now instrumented: 42 tracked boxes, a gate that permits strength, a codegen fuzzer with teeth, and a coverage denominator.
- **2026-08-29 (52)** — **★★ STRATEGIC DECISION (user-directed): BUILD a real NLL borrow checker, don't port — plus a CRITICAL RETROSPECTIVE.** The 9/39 measurement made the choice concrete: porting the old ~2.1 kLOC checker would spend a multi-session build reproducing a *known-weaker ceiling* (two-phase only for direct-read args ⇒ `v.push(v.len())` rejected) and would enshrine the front-end's ad-hoc ownership rules as the IR's ceiling — the exact over-fit the sovereignty audit exists to prevent. Our IR is MIR-shaped, which is where NLL belongs. **Two new design docs:** (1) `internal/design/borrow_checker_design.md` — the committed model: places+overlap, loans, liveness regions (=NLL), the single conflict rule that subsumes the whole conflict/reborrow/loop cluster AND unifies move/leak as `Move` accesses, two-phase borrows, cross-function elision, drop/defer as accesses, phases A–D. **★ Its §4 is the project's most defensible "beyond Rust" claim: Rust CANNOT distinguish `a[i]` from `a[j]` (why `split_at_mut` needs unsafe); we can query the octagon for `i≠j` / range-disjointness on the SAME IR ⇒ a safe `split_at_mut`, proven not asserted.** (2) `internal/design/critical_retrospective.md` — an honest audit of our own failures, whose through-line is: **we optimised against the incumbent instead of against the goal.** Findings became a tracked **CORRECTIVE BACKLOG** (C1–C6): the parity gate is a *structural mistake* (it forbids being stronger — replace with adjudicated divergence, C1, blocks everything); no execution-differential fuzzer exists for the NEW pipeline (C2 — that blind spot hid the scalar-`var` miscompile); `incomplete` is an unmeasured escape hatch conditioning every survey number (C3); the memory model was starved while the numeric domain was fed (C4 — promote S2 above VRA tuning); the effect lattice was inherited not designed (C5); the type lattice deferrals compounded every phase (C6). Also logged the flagship language gap: **no lifetime syntax** (`lain_language_limits.md` §6b) — cross-function borrow precision is capped until the front-end can state what the IR can verify.
- **2026-08-29 (51)** — **★ Endgame push: reject-parity MEASURED + a real MISCOMPILE fixed.** Pushing at 3.5 required reject-parity, which required the new ownership passes to RUN on programs the old engine exit()s on. Added the suppression seam `g_suppress_ownership` (mirrors bounds.h's `g_vra_suppress_bounds`) standing down resolve.h's dangling-return check + linearity.h's checker; set ONLY by the differential driver (`--reject`), never the compiler. **That immediately exposed a genuine MISCOMPILE in the new pipeline:** EXPR_MUT took the ADDRESS only for structs, so a scalar `var x` param travelled BY VALUE and the callee's writes were lost — `proc bump(var x i32){x=x+%1}` gave **41 instead of 42** under the new codegen. Fixed with `ir_mut_by_address()` (a mutable borrow of a COPIED type — struct or scalar — travels as an address; slice/array/ptr already share data) applied to BOTH the param lowering and the EXPR_MUT argument. New codegen now 42. **Honest endgame measurement: ownership fail-tests CAUGHT 9/39.** The 30 missed need NLL conflicting/reborrow/loop borrows, defer semantics, move variants, co-arg aliasing — i.e. 3.5 is a multi-session build of a real borrow checker, not a finishing touch. Verified after the lowering change: gate 15/15, corpus 625, VRA 523/563 & 73 fully proven (unchanged), reject 0 PROVED-ALL, IR unit tests pass, parity gate HOLDS, fuzz_trust 0-miscompile, fuzz_linear 0-unsound.
- **2026-08-29 (50)** — **Phase 3 PARITY GATE (reproducible) + it caught a real crash.** New `phase3_differential.sh` (the Phase 3 analog of vra_survey.sh): over the corpus it asserts (A) linearity+borrow raise 0 findings on old-accepted pass programs (no over-rejection) and (B) the effect row drops 0 observable effects. Building it immediately exposed a **pre-existing new-lowering CRASH** (segfault) on INDIRECT calls: `f(x)` through a fn-pointer variable has a DECL_VARIABLE callee, but the call lowering read `callee->as.function_decl` (wrong-union deref). Fixed: null a DECL_VARIABLE callee (a DECL_STRUCT callee stays — it's a constructor); hardened linearity.h vs NULL operands. Gate now: **PARITY HOLDS (0 FP / 0 dropped effects over 340 pass programs)**. No regression: gate 15/15, corpus 625, reject 0 PROVED-ALL, test_vra 19/19, test_linearity 9/9, test_borrow 4/4. The three Phase 3 analysis cores are now locked behind a reproducible parity gate — future refinements (E016, per-field, NLL lifetimes, recursion-termination) are safe to land against it, then 3.5 (authoritative + delete src/sema).
- **2026-08-29 (49)** — **★ Phase 3.1 borrow CORE — all THREE analyses now sovereign on the IR.** `analysis/borrow.h`: the dangling-reference core via escape analysis — a `return` whose pointer/slice provenance roots in a local IR_ALLOCA (traced back through elem_ptr/field_ptr/slice_data/make_slice) is a stack use-after-return. A param is a pointer value (caller's storage), not an alloca ⇒ safe; heap/global/loaded roots stop the walk (conservatively safe). test_borrow.c 4/4 (`return &local` + `&local.field` fire; param/loaded pointer quiet). **0 false positives across all 340 clean-loading pass programs** (linearity + borrow together). Pure analysis, no lowering/codegen change ⇒ corpus 625 / gate 15/15 / VRA untouched. **Phase 3 status: 3.3 effects ✓ full, 3.2 linearity ✓ (E001/E002/E003+B5), 3.1 borrow ✓ core** — the three memory-/resource-safety analyses are ported off the AST, each sovereign + unit-tested + 0-FP-differential. Remaining for full parity: 3.1 NLL lifetimes/two-phase, 3.2 E016/per-field, 3.4 recursion-termination, then 3.5 gate + delete src/sema.
- **2026-08-29 (48)** — **Phase 3.2 extended: B5 linear substrate + leak check (E003).** Added `IrType.linear` (the general ownership/multiplicity qualifier — B5), set by wrapping `ir_lower_type` over the front-end's `sema_type_is_linear` oracle so the pass reads it off the IR (no AST). Leak check: a linear OWNED POINTER/SLICE slot still live at a `return` is a dropped resource (E003) — narrowed to pointers/slices because a trivially-droppable owned STRUCT is move-tracked but not a leak (marking all MODE_OWNED over-reported on 3 pass tests); transitive struct-field leaks need per-field tracking (deferred). test_linearity.c 9/9 (leak fires on a dropped owned ptr, quiet when moved, plain i32 drop fine). **0 false positives across ALL 340 clean-loading pass programs** (E001/E002/E003); fuzz_linear 0 over-rej/0 missed/0 unsound. No regression: reject 0 PROVED-ALL, corpus 625, gate 15/15, test_vra 19/19. Phase 3.2 now: E001+E002+E003+B5; remaining = E016 branch-consistency + per-field. NEXT: 3.1 borrow/region (NLL — dangling-return the highest-value core).
- **2026-08-29 (47)** — **Phase 3.2 linearity CORE — use-after-move on the sovereign IR.** Second analysis onto the IR. `mov x` now lowers to a new `IR_CONSUME` op on x's slot (codegen no-op, VRA-ignored); `analysis/linearity.h` runs a forward MAY-move dataflow over the CFG — moved on SOME path ⇒ any later reference is a use-after-move (E001); a second consume is a double-move (E002); a STORE re-initialises the slot. `test_linearity.c` (hand-built IR, sovereign) proves E001/E002 fire, re-init clears, a move on one branch flags a use across the join, never-moved stays clean — all met. **0 false positives** across the clean-loading ownership pass corpus (new never rejects what old accepts); fuzz_linear 0 over-rej / 0 missed-violations / 0 unsound. No regression: reject 0 PROVED-ALL, corpus 625, gate 15/15, test_vra 19/19, survey 73 fully proven. This is the use-after-move CORE; leak (E003), branch-consistency (E016), per-field + linear-type tracking (needs the B5 ownership substrate on IrType) are scheduled for full parity. Phase 3 now: 3.3 ✓, 3.2 core ✓; NEXT 3.1 borrow/region (NLL) + B5 substrate, then 3.4 termination, 3.5 gate+delete src/sema.
- **2026-08-29 (46)** — **★ PHASE 3 STARTED: 3.3 Effects ported to a sovereign IR pass (F1).** First analysis moved off the AST onto the IR. New `analysis/effects.h` computes the effect LATTICE (WRITE/DIVERGE/RAISES/IO/ALLOC — the audit's F1, replacing the PURE/PROC binary) from the IR ALONE + a call-graph fixpoint (callee ⊆ caller, memoized on IrFunc). Infra: `ir_lower_module` (all bodies PLUS declaration-only extern stubs, so the module classifies extern calls without the front-end); the call lowering fills a missing callee name from the identifier so declless builtins (`panic`) are recognizable. Consumers: panic→RAISES, call unions callee effects (unknown⇒IO), DIVERGE via `vra_loop_terminates` DIRECTLY (semantic — applies to procs), extern func⇒{}/proc⇒IO, recursion cycle⇒DIVERGE. New `effects_driver.c` differential vs old `effect_full` over 362 programs: **0 functions drop an observable effect (WRITE/IO/RAISES)** — the soundness direction; DIVERGE differs on 72, ALWAYS the new engine MORE precise (VRA proves termination where the old flagged a no-`decreasing` loop syntactically). No regression: reject 0 PROVED-ALL, corpus 625, gate 15/15, test_vra 19/19, survey 73 fully proven, fuzz_trust 0-unsound/0-miscompile. NEXT in Phase 3: **3.2 linearity** (ownership/use-once, CFG dataflow) then **3.1 borrow/region** (NLL); both intraprocedural CFG passes that fit the IR (the harder interprocedural bits ride the contract layer). DIVERGE-vs-measure parity is a 3.4 (termination) refinement (carry/prove the `decreasing` measure on the IR).
- **2026-08-29 (45)** — **Short-circuit `and`/`or` guard refinement + func-only termination — survey 519→523, 70→73 fully proven (93%).** (a) **`and`/`or` guard expansion** (e26f858): `if a<n and b<n {arr[a]=arr[b]}` couldn't prove — the EXPRESSION lowering of `A and B` materializes a bool CELL and the `if` refines on the cell, hiding the numeric facts. New `ir_lower_cond_br` expands `and`/`or` in GUARD context into nested short-circuit branches so the octagon refines both operands along the taken path (STMT_IF only — while-guards keep the single header br_cond for the termination check). soa_output both guarded indices prove. (b) **Termination is func-only** (6e9c38f): the VRA_TERMINATION obligation was emitted for every loop header, but totality is a `func` requirement — a `proc` may loop forever (event loop); a future authoritative engine would wrongly reject one. Gated on IR_FUNC_PURE; test_vra's termination unit-tests now build funcs. SOUND: reject 0 PROVED-ALL, corpus 625, gate 15/15, test_vra 19/19, fuzz_trust+fuzz_strings 0-unsound/0-miscompile. **Session (40-45) total: survey 508→523 (+15 bounds, 90→93%), 60→73 fully proven (+13), all sound** — 8 general capabilities (closure-cost hang fix; MUL scaling; selective widening; relational SUB; `in`-refinement; SDIV/SREM; and-guard expansion; func-termination). Remaining tail is hard/niche: symbolic-2D nonlinear (3-var, needs polyhedra/ghost-vars), struct-field-slice length canonicalization (+ old engine also rejects the simple form), accumulation-overflow residuals (genuine risks). Precision frontier near its tractable plateau; next major lever is architectural (Phase 3: borrow/linearity/effects on the sovereign IR).
- **2026-08-29 (44)** — **`in`-refinement + div/mod-index clusters — survey 515→519, 66→70 fully proven (92%).** (a) **`in` param refinement** (4e91e97): `pos usize in text` is stored as `in_field` (not `constraints`), so it never lowered — added the entry `assume(pos <u len(text))` + its CALL-SITE dual `assert(arg_pos <u len(arg_text))` in ir_lower_call_requires (keeping the assume/guarantee invariant). in_field_forwarded 2/2. (b) **SDIV + non-const SREM** (c56304e): `i/2` lowers to signed sdiv (i32 loop var) and `i%buf.len` to signed srem w/ non-const divisor — neither had a transfer (→ unbounded index). Added IR_SDIV (x≥0,c>0 ⇒ 0≤r≤x, like udiv) and extended IR_SREM (a≥0,b>0 ⇒ 0≤r<b, like urem). div_index 7→8/10, mod_index 20→21/22 (residuals = genuine `acc+=a[..]` accumulation overflow). SOUND throughout: reject 0 PROVED-ALL, corpus 625, gate 15/15, test_vra 19/19, fuzz_div+fuzz_loop+fuzz_offset 0-unsound. **Remaining:** symbolic-2D nonlinear `i*w<h*w` (hard), struct-field-slice length canonicalization (`l.src[i]`) + its termination, soa short-circuit `and`-guard refinement through a bool cell, accumulation-overflow residuals.
- **2026-08-29 (43)** — **★ Reverse index `a[L−1−i]` UNLOCKED: relational SUB (symbolic length).** `L − i` with symbolic L is a 3-var relation the interval SUB transfer can't capture, so the octagon lost the reverse index's no-underflow lower bound — even though it ALREADY held `i ≤ L` from the loop guard. Fix (5c0f9f7): read the octagon's OWN a↔b difference (DBM closed at the site) and transfer it EXACTLY — `r = a−b` gets `r ≤ (a−b ub)` and `r ≥ −(b−a ub)`; the overflow check for SUB refines its operand-interval range with the same relation. From `i ≤ L`: `L−i ≥ 0` (no underflow) and `< L` (in bounds). two_pointer_reverse 15→17/19, reverse 15/17, vra_reverse_plain_slice index proven (residual is a genuine `s += a[..]` accumulation overflow). **Survey 510→515, 62→66 fully proven (91%).** SOUND: reject 0 PROVED-ALL, corpus 625, gate 15/15, test_vra 19/19, fuzz_offset+fuzz_loop 0-unsound. This session (40-43): +7 bounds / +6 fully-proven, all sound; two general octagon capabilities added (MUL scaling, relational SUB) + selective widening (nested loops) + the 20s hang fix. **Remaining clusters:** nested struct-field arrays (`m.data[i]`), symbolic-2D nonlinear `i*w<h*w`, div/mod-index, reverse-loop termination.
- **2026-08-29 (42)** — **★ 2D const-stride UNLOCKED: IR_MUL scaling + selective widening (nested-loop invariants).** Two fixes toward the audit's S2 / North-Star 2D gap. (a) **IR_MUL interval scaling** (d865074): MUL had no transfer (→ default forget), so `i*8` was unknown; add sound `r=x*c` scaling (c>0 → [c·lo,c·hi], overflow-guarded via __int128 `vra_safe_scale`). (b) **Selective widening** (a4d13aa): the classic nested-loop flaw — an outer induction var (invariant in the inner loop, but growing 0..k across OUTER iterations) was blown to +∞ at the INNER header (traced: the cell's upper bound absent in the inner body). Fix: at header H widen ONLY octagon entries touching a var MODIFIED in L(H) (a `store %c` in the loop's block-id range [H, max back-edge src] — sound for the structured CFG); invariant vars keep their joined bound. Result: **matrix_const_stride `a[i*8+j]` 3/7 → 7/7 FULLY proven**, nested `a[i]` 5/5; survey 508→510, 60→62. **Soundness process win:** a first cut used a NARROWING pass — the reject-survey caught it red-handed with **7 FALSE proofs on OOB fail-tests** (narrowing against ONE edge, not the join of incoming; a single edge is tighter than the true invariant). Reverted to selective widening (correct). Verified: reject 0 PROVED-ALL, corpus 625, gate 15/15, test_vra 19/19, fuzz_loop+fuzz_trust 0-unsound, masked_ring still 24ms. **Remaining:** matrix_symbolic_2d needs genuine nonlinear `i*w < h*w` (symbolic stride) — the hard relational case, next.
- **2026-08-29 (41)** — **★ ROBUSTNESS: killed an O(dim³)-per-instruction closure — 20s analyzer HANG → 41ms.** Empirical accept-side scan (all indexing `tests/*.ln`, per-file timeout) found `masked_ring_index_pass` (64-slot linear-probing insert) HANGING the driver ~20s — the survey couldn't complete. Not widening (both fixpoints ≤3 sweeps): `oct_forget` called `oct_close` (O(dim³)) as its first line and runs on ~every instruction's result; a 64-elem array literal has dim≈400 × ~200 instrs ⇒ ~1.2e10 ops in BOTH fixpoint and final pass (newly tripped by Fix B/C's extra assert instructions pushing dim over the knee). Fix (01a3670): `oct_forget` no longer closes — it is only ever called on the FRESH SSA result being defined (no prior constraints ⇒ nothing to materialize ⇒ sound + lossless). Closure restored precisely where needed: `vra_assign_copy` closes once (a loaded value inherits its cell's loop invariant — infrequent), the final pass already closes per-block + per-obligation, and a **const index into a FIXED array** is decided from constants (skips the close — the array-literal fast path). Precision UNCHANGED: survey back to **508/563, 60 fully proven** (7 subslice/guard programs a first cut regressed are recovered), test_vra 19/19, corpus 625, gate 15/15, reject 0-false, fuzz_loop+fuzz_bsearch 0-unsound. Also profiled: simd_lexer/calculator/tokenizer ~2× faster. **Empirical index-bounds gap clusters** (next precision targets): 2D `a[i*w+j]` (matrix_*), reverse `a[n-1-i]` (two_pointer/reverse), nested struct-field arrays (soa/struct_field_slice), div/mod index, `in`-guard propagation — several map to the audit's S2 (rank-N strided regions).
- **2026-08-29 (40)** — **★ SEMANTIC-INDEPENDENCE AUDIT (user-prompted: "is the IR over-fit to Lain? other langs may not have slices").** Established the criterion the A5 litmus doesn't cover: *structural* independence (no front-end headers) ≠ *semantic* independence (no Lain policy in the primitives' meaning). New litmus: could safe-C **and** safe-Fortran **and** safe-Rust lower to primitive P without inventing a Lain-shaped node? Audited **every** primitive in `ir.h` (full table in `internal/design/ir_semantic_independence.md`). **Verdict: the semantic core is CLEAN** — no Lain *policy* smuggled into integer/memory/aggregate/CFG/verification primitives (checked explicitly: no MODE_* ownership, no overflow policy, no niche, no UFCS/comptime reach the IR; the slice is the *universal* bounded-region, precedent = Rust MIR fat pointers). Findings are **missing generality, not contamination** (⇒ "generalize upward"): **S2** `IRT_ARRAY`/`IRT_SLICE` are rank-1/contiguous/static — no multidim/stride/rank ⇒ can't serve Fortran sections AND manufactures our own 2D `a[i*w+j]` gap (generality+power converge on ONE strided rank-N region primitive); **F1** `IrFunc.kind`=PURE/PROC is Lain's binary effect vocabulary ⇒ replace with the effect row; **S1** `slice_sentinel` is a refinement (shared w/ Zig/C) baked as a core bool ⇒ move to the refinement layer; **noalias** one assume emitted opt-in (C `restrict`) / opt-out (Fortran default). Adversarial Fortran+Rust lowering sketch confirms both land on the same clean core + same 3 general gaps. Disciplines + scheduled items in the doc §6/§7 (all additive, none a rewrite). Principle adopted: **adversarial language selection** — design memory/verification primitives against Fortran+Rust, not the C-likes.
- **2026-08-29 (39)** — **★ ASSUME/GUARANTEE SOUNDNESS AUDIT + 2 front-end-trust leaks closed (user-prompted: "don't corrupt the IR because of the front-end").** Every `assume` the IR injects from a front-end fact MUST be paid for by an `assert` the IR itself discharges — else a sketchy front-end corrupts the octagon. Audited all 4 assume classes: type-interval (sound by type), requires (dual existed), and TWO LEAKS: **(B) return `ensures`** — caller assumed `result OP m` but nothing checked the callee returns it → **FIXED (53cbb48):** callee emits `assert(retval OP rhs)` at each `return`, proving its own postcondition (pick/lo discharge check-free); **(C) dependent length** — callee assumed `len(out)==m`, no caller check (masked by old-engine E087) → **FIXED (cf74066):** call-site `assert(len(arg) relop resolve(expr))` via recursive `ir_resolve_len_expr` (handles arithmetic `src.len-1`), fail-closed if unresolvable; validated `writeN(4,buf@len4)` → both length asserts PROVEN. Asserts are pure obligations ⇒ can't add unsoundness. **Invariant now holds: IR soundness no longer depends on trusting the front-end.** Also (738d3d6) robust `.len` by lowered IR type ⇒ arithmetic dependent lengths (`i32[src.len-1]`), **accept-side 508/563 (90%), 60 fully proven.** Corpus 625, gate 15/15, reject 0-false, fuzz 300 iters 0-unsound.
- **2026-08-28 (37)** — **Robust refinement-RHS lowering — dependent_array unblocked, accept-side 89%.** Root cause: refinement/size exprs come through sema UNTYPED (front-end gap #3 in `internal/design/lain_language_limits.md`), so `ir_lower_expr(out.len)` in `i32[out.len]` hit the AST `.len` path on a null type → `const 0:unit` placeholder → whole function `incomplete` (dependent_array was 0/3). Extracted `ir_lower_refinement_rhs` (a literal gets the fallback type; `x.len` resolves by the LOWERED value's IR kind, never the missing AST type) and used it for param refinements + dependent lengths. `dependent_array` 0/3→**4/6**; `vadd(var out i32[], a i32[out.len], b i32[out.len])` now analyzed. **Accept-side 497→503/563 (89%), 60 programs fully proven.** Corpus 625, gate 15/15, reject 0-false, test_vra 19/19.
- **2026-08-28 (36)** — **Phase 2.9 B2 COMPLETE — the contract loop closes soundly (requires + the assert-discharge mechanism).** Added `IR_ASSERT` + `vra_icmp_holds` (the discharge dual of guard-refine — does `a cmp b` hold in the closed octagon?) + a `vra_check_assert` consumer + `VRA_PRECOND` obligation class. A callee's param refinements now become **call-site `assert`s the CALLER must discharge** (`at(xs,2)` proves `2 < xs.len` since `xs:i32[4]`) via `ir_lower_call_requires` + `ir_resolve_contract_rhs` (substitutes callee param→arg: `m`→arg, `a.len`→slice_len(arg)). This CLOSES the contract: caller proves `requires`, callee `assume`s them at entry (now justified), caller `assume`s `ensures` after the call — the verification-native loop, self-hosting-sound. Preconds are a distinct class so they don't muddy the bounds survey (still 497/563). Corpus 625, gate 15/15, reject 0-false, test_vra 19/19.
- **2026-08-28 (35)** — **B4-lite: dependent param bounds `i < a.len` — accept-side 492→497/563 (88%), 59 fully proven.** Generalized `ir_lower_param_refinements` to resolve ANY refinement RHS — a literal (kept the param-typed const path; the constraint expr is untyped so `ir_lower_expr` would give it `unit`), a length `a.len` (→ `slice_len(a)`), or another param `m` — and moved it to the second param pass so the RHS resolves. So `func at(a i32[], i usize < a.len)` now emits entry `assume(i < slice_len(a))` and proves `a[i]`. Combined with (34) dependent lengths + (33) ensures, the verification-native assume layer took the accept-side **411→497 (81%→88%)** this session, all reject-0-false / fuzz-0-unsound. Sound relative to the old engine's call-site precondition enforcement (the `requires`-assert half formalizes it for self-hosting). Corpus 625, gate 15/15.
- **2026-08-28 (34)** — **★ B4-lite: dependent lengths — accept-side 467→492/563 (87%).** A slice/dynamic-array param whose type carries a length constraint (`out i32[m]`, `i32[arr.len]`, `i32[>= n]`; AST `size_expr`+`size_relop`) now lowers to an entry `assume(slice_len(p) relop <expr>)` — the runtime length is connected to the symbol via the assume layer (a second param-pass, after all params are in scope, so `m`/`arr.len` resolve). This unlocks the whole dependent-length class: `out[n]` under `n<m` with `len(out)==m` discharges. **+25 bounds obligations, +7 fully-proven programs (47→54).** Full B4 (refinement predicates ON `IrType`, symbolic slice-length identity) is the remaining keystone; this assume-based slice does the highest-value slice of it. Corpus 625, gate 15/15, reject 0-false, test_vra 19/19.
- **2026-08-28 (33)** — **Phase 2.9 B2 (ensures) — INTERPROCEDURAL contracts via the assume layer.** A callee's return refinement `func f(..) usize <= m` now lowers, at each call site `v = f(args)`, to a **post-call `assume(v OP <that>)`** — the caller *learns* the ensures. rhs resolves through the callee's params to the actual argument (`<= m` with `m`=the 2nd arg → `assume(v <= arg1)`). This is verification-native interproc: `bounded_counter_return` (`count = fill(var buf, 16)` ensures `<= m`, then `buf[0..count]`) went to **8/10** — the subslice `hi=count ≤ 16 = len` now discharges. Sound relative to the old engine (which verifies the callee's ensures; a self-hosting replacement will also verify ensures + assert `requires` at call sites — the requires half is next). Corpus 625, gate 15/15, reject 0-false.
- **2026-08-28 (32)** — **★ PHASE 2.9 MOVE A COMPLETE — the IR is SOVEREIGN (litmus PASSES).** `src/ir/*` and `src/analysis/*` now compile with **zero front-end headers** (proven: preprocessed litmus TU pulls in no `src/ast.h`/`token.h`/`lexer.h`/`parser.h`). ir.h imports only `def.h`+`arena.h`. Introduced **`IrName`** (IR-owned, **interned** — bytes copied, so the AST is freeable at runtime too); replaced `IrType.struct_decl:Decl*`→`sname:IrName*`, `field_names:Id**`→`IrName**`, `aux.callee:Decl*`→`IrName*` (function name), `aux.struct_decl` dropped, `IrFunc.name:Id*`→`IrName*`, `IrFunc.src_decl:Decl*`→opaque `void*`, `IrModule.types`→opaque. lower.h is the SOLE AST↔IR adapter (interns via `ir_intern`). `test_vra.c`/`fuzz_vra.c` are now themselves AST-free (pure IR+analysis). Caught one field-order bug (`ir_field_index` read `field_names` as `Id*` — different layout → struct field reads returned 0). Verified: gate 15/15, struct round-trips, reject 0-false, **fuzzer 600k trials 0-unsound**, corpus 625, accept-side 465/563. The soundness authority now stands on its own.
- **2026-08-28 (31)** — **Phase 2.9 B1 STARTED — the verification `assume` node, and the first AST leak SEVERED.** Added `IR_ASSUME`/`IR_ASSERT` (op[0] = a bool that holds / must be discharged). Param refinements now lower to **entry `assume(icmp(param, const, op))` nodes** (`ir_lower_param_refinements` — lowering is the AST adapter, the IR then owns the fact). The VRA's transfer consumes `IR_ASSUME` by refining the octagon (reusing guard-refinement), and **the `f->src_decl` read is DELETED** (with the AST-reading `vra_seed_constraint`). `vra_while_lt_scalar` still 5/5 — but now the refinement is `assume %3` visible in the IR dump, not fished from the parse tree. `assume`/`assert` emit as no-op runtime code. Corpus 625, gate 15/15, reject 0-false. This is the pattern the whole verification layer follows: front-end fact → IR assume/assert → analysis consumes IR, never AST.
- **2026-08-28 (30)** — **★ MAJOR REFRAME: IR SOVEREIGNTY (user-directed).** Committed the thesis that **Lain-IR is the semantic + soundness AUTHORITY** — a total, self-contained, verification-native IR that is simultaneously the codegen substrate; front-end and backend are replaceable clients. See the new "★ THE REFRAME" section + **Phase 2.9** (Move A: sever the AST dependency, litmus = `src/{ir,analysis}/*` compile without `ast.h`/`token.h`; Move B: `assume`/`assert`/`requires`/`ensures` + `opaque`/havoc replacing `incomplete` + refinements/regions as first-class IR type structure). Reorder: **2.9 BEFORE Phase 3** (porting borrow/linearity onto a still-AST-leaking IR would spread the coupling). Precedent noted: closest are Rust MIR, CompCert, Viper/Boogie; LLVM is the opposite (UB-exploiting). The `f->src_decl` refinement-seeding leak (added this session) is exhibit A of the coupling to remove. STARTING execution now with B1 (`IR_ASSUME` + refinements-as-assumes).
- **2026-08-28 (29)** — **Param refinement constraints seeded into the entry octagon.** A refined param `n u32 < 4096` (or `i usize < 4`, `start u32 < 4090`) carries its constraint on the AST decl; the VRA now walks `f->src_decl`'s params alongside the IR params and seeds each `param OP <const>` into the entry octagon. So `src[i]` under `i < n` with `n < 4096 = src.len` proves — `vra_while_lt_scalar` 2/3 → **5/5**. Sound relative to the old engine (which enforces the precondition at call sites; a self-hosting replacement will need call-site precondition checks too, a Phase-3 item). Accept-side **454→465/563**, fully-proven **43→47**; reject-side **0 false proofs**.
- **2026-08-28 (28)** — **Short-circuit `and`/`or` — a real miscompile fixed, and guard-indexed accesses now prove.** The lowering evaluated BOTH operands (`x & y`), which is wrong when the right operand guards a deref/index (`p != null and p.x`, `i < len and a[i]` would touch `p.x`/`a[i]` unconditionally → OOB/crash). Now lowered with real control flow (a bool result-cell; the right operand's block is entered only when the left doesn't already decide). Bonus: the `br_cond` on the left condition lets the octagon refine the eval-right block, so `i < a.len and a[i]…` PROVES `a[i]` in the guard. Self-checking `shortcircuit1.ln`: `check(a,5)` must NOT touch `a[5]` (short-circuit) → runs exit 0; VRA proves the guarded `a[i]`. Unknown binary ops now fail-closed. Corpus **625**, gate **15/15**, reject-side **0 false proofs**.
- **2026-08-28 (27)** — **Variable-divisor `÷`/`%` transfers — the wrap-around index `a[i % n]`.** Extended the modulo transfer beyond a constant divisor: `x % b` ⇒ `0 ≤ r < b` (relative), and added `x / b` ⇒ `0 ≤ r ≤ x`. Sound by the standard assume-defined-execution rule — `b = 0` is a separately-flagged div-by-zero, so any execution reaching past the op has `b > 0`, and closure soundly derives `b ≥ 1` and `r < b`. This proves the ring-buffer idiom `buf[i % buf.len]` and `a[i / k]`: **mod_index 14/16→15/19, div_index 4/5→6/9**. Fuzzer still 0-unsound (its const modulo divisor is always ≥ 1). Old corpus **624**.
- **2026-08-28 (26)** — **Array comprehensions lowered — unblocks the array-of-struct initializer idiom.** `var a T[N] = [ body for i in range ]` now desugars to a fill-loop `i=lo; while i<hi { a[i]=body; i+=1 }` (reusing the array slot + element store; the body may itself construct a struct, e.g. `[Token(0,0) for x in 0..64]`). Any *other* unmodeled aggregate initializer now fail-closes (`incomplete`) instead of silently leaving the array uninitialised. `comp1.ln` (`[i as i32 for i in 0..5]` then `for v in a`) round-trips lower→C→run exit 0 (sum=10). **calculator 0/12-incomplete → 5/42 faithfully analyzed** (the comprehension was its only unlowered construct). Corpus **624**, gate **14/14**, reject-side **0 false proofs**.
- **2026-08-28 (25)** — **Integer/char `case` match lowered (if-chain); enum match fails closed.** Lain's match is `case <value> { P: body … else: body }`. An integer/char match on literal + range patterns now lowers to a faithful comparison chain (each arm a block; ranges → `v≥lo ∧ v<hi`); `case n { 0: … 1: … else: … }` round-trips lower→C→run exit 0 (`match1.ln`). Enum/ADT matches (tag+payload) are NOT lowered → the function is marked `incomplete` and every proof suppressed (verified on the error-union `case r { NotFound: … }` → 0/0). Corpus **623**, behavioural gate **13/13**, reject-side still **0 false proofs**. Remaining dropped constructs: enum-match, defer.
- **2026-08-28 (24)** — **`in`-guard lowered — `while i in data { data[i] }` now proves.** `i in data` is a binary operator (prec 7, valid-index guard `0 ≤ i < data.len`); the lowering didn't recognize it and fell to a garbage `AND`. Now it lowers to a real `icmp ult i, len(data)` (the ≥0 half comes from i's type/flow), so guard refinement and the termination check both engage. Self-checking `inloop1.ln` round-trips lower→C→run exit 0 (sum=100). Accept survey **424→432/545**; corpus **622**; behavioural gate **12/12**. Remaining dropped constructs: match/defer.
- **2026-08-28 (23)** — **`for` loops lowered — the biggest faithfulness gap closed.** `STMT_FOR` was the dropped construct that (post-fail-closed) forced `incomplete` on every for-loop program. Now both forms desugar to counted `while` loops: `for i in lo..hi { }` → `i=lo; while i<(=)hi { body; i+=1 }`; `for v in arr { }` → `i=0; while i<arr.len { v=arr[i]; body; i+=1 }` (slice or fixed array; optional index binding). Self-checking `forloop1.ln` (both forms) round-trips lower→C→run **exit 0** (5×10=50). The affine reject case is now caught FAITHFULLY (for-loop lowered ⇒ x=30 unbounded ⇒ `arr[x]` refused on its own merits, no longer via `incomplete`). Accept survey **403→424/540** bounds proven (for-loop programs are analyzed instead of suppressed); reject-side still **0 false proofs**; behavioural gate 11/11; old corpus **621**. Remaining dropped constructs (match/defer/`in`-loop) still fail-closed.
- **2026-08-28 (22)** — **★ REJECT-SIDE DIFFERENTIAL — found & fixed two real soundness bugs.** Added a minimal opt-in seam (`g_vra_suppress_bounds` in `sema/bounds.h`, default OFF — legacy behavior byte-identical) so the new engine can analyze programs the legacy bounds check `exit()`s on; `vra_driver --suppress` + `vra_reject_survey.sh` run it over the corpus's E085 bounds-`_fail` tests. First pass exposed **3 programs the new VRA falsely PROVED**. Fixed both root causes: (1) **subslice end-bound was unchecked** — `s[0..9]` on a len-3 slice only checked the *start* `0<3`; now `vra_check_subslice` checks `lo≥0 ∧ hi≤len` and the start GEP is skipped (it's pointer arith, not an access). (2) **fail-OPEN lowering** — `STMT_FOR`/match/unhandled-expr were silently dropped/placeholder'd, so `for i in 0..10 { x+=3 }; arr[x]` lowered to `arr[0]` and got "proved"; now any dropped/placeholder construct sets `IrFunc.incomplete` and the VRA **suppresses every proof** over infaithful IR (fail closed). Result: reject-side **0 false proofs** (33 refused, 10 no-obligation/lowering-gap); accept-side 411→403 (the 8 lost were unsound proofs over incomplete IR — a net soundness win). Unit 19/19, fuzz 0-unsound, old corpus 620 (seam inert). The differential earned its keep on day one.
- **2026-08-28 (21)** — **Termination consumer — the VRA now discharges all FOUR `func`-safety obligations.** A structured loop is proven terminating when its guard cell is updated by EXACTLY ONE well-formed step `cell = load(cell) ± c` whose direction drains a loop-invariant bound (rise toward an upper / fall toward a lower); the single-store rule is the conservative soundness guard (any other write ⇒ not proven). Proves `i+=1 while i<n` / `i+=2 while i<=n`, refuses `i+=0` (stuck) and `i-=1` (diverges). `VRA_TERMINATION` check kind + `defblk` (per-value defining block) added. `test_vra.c` = **18/18**. Fuzzed sound: extended `fuzz_vra.c`'s interpreter to report halting and added phase 3 — **100k random loops (step ∈ {0,1,2}), 66740 proven terminating, 0 unsound** (every "terminates" verdict actually halted). The engine now covers bounds + overflow + div0 + termination, each fuzz-validated (200k+300k+100k trials, 0 unsound across all three).
- **2026-08-28 (20)** — **Nonlinear mask/modulo/shift transfers — the check-free `a[x & (N-1)]` idiom.** Added transfer functions for the catalog's "type-b" small-nonlinear cases: `x & c` (c≥0 const ⇒ 0≤r≤c — the power-of-two mask/gather idiom, bounded for ANY x), `x % c` (UREM ⇒ [0,c−1]; SREM ⇒ [−(c−1),c−1], tighter if x≥0), `x >> k` (logical shift of x≥0 ⇒ [0,x]). `test_vra.c` now **13/13**: `a[x&7]` over `a[8]` proven, `a[x&15]` over `a[8]` refused (sound). Extended `fuzz_vra.c`'s generator+interpreter to emit `i&c`/`i%c` index forms — re-fuzzed **200k trials, still 0 unsound** (54466 proofs all concrete-safe). These are the SIMD-gather / hash-bucket patterns; the old engine got them via a hand-coded recognizer, here they fall to a couple of sound interval facts.
- **2026-08-28 (19)** — **★ SOUNDNESS FUZZED — 200k random programs, zero false proofs.** `src/analysis/fuzz_vra.c`: generate random counted-loop index programs *as IR* (bypassing the frontend, so UNSAFE ones the old sema would `exit()` on are included), run the VRA (abstract), and cross-check **every "proven check-free" verdict against a concrete interpreter of the same IR** (ground truth). Result: **200000 trials, ~68k genuinely out-of-bounds, VRA proved 74047 accesses — all 74047 concretely safe, 0 UNSOUND.** This is the gold-standard abstract-vs-concrete check and it directly de-risks the transfer functions + guard refinement + widening (where a soundness bug would otherwise hide). Caught its own harness bug first (elem_ptr builder returns the value not the instr → matches silently no-op'd). A second phase brute-forces the overflow consumer's interval arithmetic (`vra_arith_range` must over-approximate `{a OP b}`) — **300k trials, 0 failures**. The new proof engine is now validated at THREE levels: the octagon domain (`test_octagon.c`, 40k γ-trials), the whole bounds analysis (`fuzz_vra.c` phase 1, 200k concrete-differential), and the overflow logic (phase 2, 300k over-approximation).
- **2026-08-28 (18)** — **Relational breadth + the 2.6 accept-side differential.** Added the two-pointer relational proof (`a[i]` under `i≤j ∧ j<len` — transitive octagon closure) and seeded integer-param type ranges into the entry octagon (so a `usize` param is known ≥0); `test_vra.c` now **11/11** (bounds, overflow, div0, sliding-window, two-pointer). Built `vra_survey.sh` (committed) — the ⊇-accepts differential: over every old-accepted indexing program, the new engine proves **411/510 bounds obligations (81%)**, 40 fully, from-scratch. Two precision wins from the survey punch-list: slice-length propagation through make_slice + slice cells (`s=a[lo..hi]; s[k]`), and fixed-array-value length from the type (a `u8[16]` param). Remaining gaps are lowering-side (nested array-in-struct, global arrays, `in`-loops) or off-North-Star (SIMD). Old engine untouched + green.
- **2026-08-28 (17)** — **★ THE NEW ENGINE DOMINATES THE OLD — proves a pattern the old engine REJECTS.** The whole justification for the octagon rebuild, demonstrated: the sliding window `a[i+1]` under `while i+1 < a.len`. The OLD engine rejects it (`[E085]` "cannot prove index within bounds", index `i+1` range [1,MAX]) — its interval-plus-recognizers approach structurally can't relate `i+1` to `len`. The NEW octagon engine PROVES it check-free: the guard's `i+1` and the index's `i+1` both equal the shared `i`-cell +1, so closure carries the guard fact `i+1 < len` to the index. Hand-built in `test_vra.c` (sema exits on the reject, so we can't route it through the driver). Full `test_vra.c` now **10/10**: bounds (2 prove / 3 refuse), overflow (1/1), div0 (1/1), relational sliding-window (prove). This is Phase-2 goal 2.8's "⊇ accepts" shown in miniature. Next: more relational patterns (two-pointer `i≤j`, reverse `a[n-1-i]`, Lomuto), then the full 2.6 differential over the corpus + fuzzers.
- **2026-08-28 (16)** — **Overflow + div-by-zero consumers — the VRA now discharges BOTH flagship duties.** Extended `vra.h` with the ℤ-range overflow obligation (§2.6): at a CHECK-mode `+`/`−`/`×`, compute the result's ℤ range from the operands' (type interval ∩ octagon), in `__int128` so the checker can't wrap itself, and require it to land back inside the operand type; and div-by-zero (divisor range excludes 0). `VraCheck` is now tagged (bounds/overflow/divzero). `test_vra.c` proves-and-refuses each: `u8 x+x` proven under `x<100` / refused unguarded, `i32 a/b` proven under `b>0` / refused unguarded — **9/9**. Driver on the real loops: fixed-array `2/2` (bounds + `i+1` overflow both proven), slice loop honestly `1/3` (bounds proven; `s+a[i]` is a real unbounded-accumulator overflow, `i+1` is the usize/2^64 int64-domain gap — both SOUND, never false-proven). Next: refinement-type + termination consumers, then 2.6 differential accept/reject over the corpus.
- **2026-08-28 (15)** — **★ THE NEW ENGINE PROVES ITS FIRST BOUNDS — end to end, and it is SOUND.** Wired the octagon domain into a CFG fixpoint with transfer functions + guard refinement + a bounds consumer (`src/analysis/vra.h`, 2.3–2.5). Variable model: one octagon var per IR value id; an alloca id doubles as its scalar memory cell, so `i=i+1` (load/add-const/store) becomes the difference fact that survives the loop — no mem2reg needed yet. Widening at the loop header (back-edge) + guard refinement (`i<n` ⇒ octagon constraint on the taken edge) + slice-length canonicalization (the guard's `a.len` and the index's length are the *same* octagon variable). Two proof harnesses: `vra_driver.c` lowers a real Lain file and reports each index — **both flagship patterns PROVEN**: fixed-array `while i<8 { a[i] }` and slice-scan `while i<a.len { s+=a[i] }`. And `test_vra.c` hand-builds UNSAFE IR the old sema would `exit()` on, confirming the engine **REFUSES** them: off-by-one `i<=8`/a[8], over-bound `i<16`/a[8], unguarded param index — all correctly not-proven. This is the whole rebuild thesis demonstrated: typed IR + octagons + fixpoint = a clean, sound, check-free proof engine. Octagon test still green (40000 trials), Phase-1 behavioural gate 10/10, old engine untouched. Next: overflow/div0 consumers, φ/mem2reg, then differential accept/reject over the corpus (2.6).
- **2026-08-28 (14)** — **PHASE 2 STARTED — the octagon domain (2.1) is built and brute-force sound.** `src/analysis/octagon.h`: the relational numeric domain the whole rebuild is *for* — conjunctions of `±x ± y ≤ c` as a DBM over 2n ± dimensions (Miné's encoding, coherent twins maintained by the setters). Implements Floyd–Warshall + strong (integer-tight `⌊c/2⌋`) closure, ⊥ detection, join (entrywise max — **the φ-merge `src/sema/` never had**), meet, widening (loop-convergent), order, forget/projection, and the interval read the bounds/overflow consumers will use. `test_octagon.c` validates every claim by **brute force over concretization γ** (the soundness yardstick from the design doc): closure preserves γ, ⊥ is sound, join/meet/widen over-approximate, meet is exact intersection, widening converges — **40000 random trials over ℤ³** + hand-built tightness cases (`x∈[2,5] ∧ x−y=1 ⇒ y∈[1,4]`, transitive `x−z≤5`), all pass. This is the keystone of the payoff phase. Next: 2.3 CFG fixpoint + 2.4 transfer functions on the IR, then 2.5 consumers (bounds/overflow/div0).
- **2026-08-28 (13)** — **Type aliases resolve to their base type.** `type NonZeroI32 = i32 != 0` used as a type lowered to opaque `struct` (→ `void*`, breaking arithmetic); now `ir_lower_type` resolves a `DECL_TYPE_ALIAS` to its runtime base (the refinement is the VRA's concern, not the representation) — the leftmost leaf of the RHS, which sema has resolved into an `EXPR_TYPE` wrapper (that was the missing piece). `double(NonZeroI32)` is now a clean `i32` param, not a materialized struct. Self-checking `inc(Small=41)==42` exits 0. **Coverage OK 303→306/376 (81%)**; unblocks the VRA refinement tests Phase 2 will lean on. Old engine green (**620**). **The core imperative language now round-trips end to end** — every construct the octagon VRA needs (all int types + ℤ/overflow-split arithmetic, arrays, slices, subslices, strings, structs by-value/by-ref, control flow, calls, casts, globals, borrows) has a passing self-checking test. Remaining tail is off the bounds/overflow North Star: SIMD, fn-pointers, Result/error monads, generics, nested array-in-struct.
- **2026-08-28 (12)** — **Structs — the last core aggregate — end to end, incl. by-reference mutation.** `IRT_STRUCT` is now self-contained (carries its lowered field types + names). Lowering: struct-name → decl resolution (memoized in LowerCtx so a self-referential field can't loop); field read/write via `IR_FIELD_PTR`; construction `Point(1,2)` → `IR_STRUCT_NEW` (compound literal); a by-value struct param materializes to a slot, a **`var` struct param is a by-reference pointer** to the caller's storage (`MODE_MUTABLE`) so field writes propagate; fixed-array `.len` folds to the constant N. Also lowered the borrow/ownership expression wrappers that gated this: `EXPR_MUT` (`var lv` → the aggregate's address), `EXPR_MOVE`, `EXPR_ADDR` (`&`), `EXPR_DEREF` (`*`). Backend: a **unified type-decl emitter** — transitively collect every struct/slice, forward-declare all structs, then slice typedefs (which only need the struct *pointer*), then full struct bodies in reverse-discovery order (a slice-of-`Token` no longer hits `unknown type name`). Round-trips: `dot(Vec(3,4))==25`, `field_construct`, and `push_n(var v)` mutation (len 0→20) all exit 0. **Coverage OK 295→303/376 (81%)** — structs net-positive over the old `void*`+placeholder that compiled to garbage. Old engine green (**619**). Deferred: nested array-in-struct indexing, type aliases (`type M = i32` → `void*`), SIMD, fn-pointers, Result/error monads.
- **2026-08-28 (11)** — **Subslices `xs[lo..hi]`.** An `EXPR_INDEX` whose index is an `EXPR_RANGE` lowers to a slice value `{ &base[lo], hi-lo }` (+1 if `..=` inclusive) via the existing `elem_ptr`+`make_slice`; open ends default (`lo`→0, `hi`→ array's const N or the source slice's `slice_len`); works over both a fixed array and a slice source. `xs[1..4]` then `s[0]+s[2]` round-trips to 60; self-checking corpus test exits 0. **Coverage OK 285→295/375 (79%)**. Old engine green (**618**). Remaining GCC_FAIL now a diverse long tail: SIMD vectors (7), fn-pointers, Result/error enums, FFI string→char* decay in variadic printf, and **structs** (the last core aggregate — next).
- **2026-08-28 (10)** — **Coverage harness sharpened + top-level constants fold.** Switched the probe to **compile-to-object** (`gcc -c`) so extern/FFI linking (the documented `-Dlibc_printf=printf` recipe) stops masquerading as a lowering gap: real coverage is **OK=284/373 (76%)**. Then closed the top-level-constant gap: a module-level `NAME T = expr` referenced in a function used to lower to a placeholder `const 0 : unit` (→ `void` locals). Now `ir_find_global_const` (threaded `globals` + `const_depth` guard into LowerCtx) folds a const reference to its initializer, **recursively** (`STEP = MAX/MIN` → `100/5`). Subtlety: sema qualifies the *reference* as `<defining_module>_NAME` while the decl keeps the bare name — match both spellings. Self-checking test `100-5+STEP==115` runs exit 0. Old engine green (**617**). Global *arrays* (`FLAGS u8[8]=[…]` indexed) still deferred. Next remaining GCC_FAIL clusters: array `.len` (should be constant N), unit-typed params (`void` param), a few slice/FFI arg-type mismatches.
- **2026-08-28 (9)** — **Differential coverage harness + strings.** Built `ir_coverage.sh` (probe, in scratch): run the NEW pipeline over every corpus program the old engine accepts, categorize OK / GCC_FAIL / LOWER_CRASH. **Baseline 178/372 (48%) of accepted programs already emit compilable C** — strong Phase-1 floor. The punch-list was unambiguous: **90× string literals** lowering to a placeholder `const 0` (`Slice_u8 ← int`). Fixed: `IR_STR_CONST` (aux.str bytes) reuses C's own static string storage — `(uint8_t*)"…"` (octal-escaped) — then `make_slice(data, len)` in `u8[:0]` context. Builder `ir_str_const`; lower.h EXPR_STRING + EXPR_CHAR; emit_c.h `ir_emit_cstr`; dump.h op name. **90→2 Slice_u8 failures**; round-trips lower→C→run exit 0. Remaining top gaps now visible: FFI/`extern` calls (need the documented `-Dlibc_printf=printf` recipe — linking noise, not a lowering gap) and **top-level constants** (globals referenced in a fn lower to `const 0` = wrong value — the real next gap). Old engine green (**616**).
- **2026-08-28 (8)** — **Slices (fat pointers) round-trip end-to-end — the flagship VRA shape now flows the new pipeline.** `func sum(a i32[]) { while i<a.len { s += a[i] } }` lowers to `slice_len`/`slice_data`/`elem_ptr`, and a caller's fixed array **decays** to a slice at the call site (`make_slice base, N`). Builders `ir_slice_data`/`ir_make_slice` (build.h); lower.h indexes a slice through `.data`, slices are first-class value slots (NOT the array "decay to base" path — fixed `ir_type_is_agg` to arrays only), CALL lowering inserts array→slice decay from the callee's param types; emit_c.h emits one `typedef struct { T* data; size_t len; } Slice_<tag>` per distinct slice type + `.len`/`.data`/make_slice. Read (`sum`) and **write** (`fill: a[i]=v` through a slice param, mutating the caller's array) both round-trip lower→C→gcc→run, exit 0. Also fixed a latent lowering bug: comparison signedness came from the result type (bool) → every `i<len` mistagged `slt`; now from the operands (`ult`) — matters for the octagon transfer functions. Old engine green (**615**). Next: structs (fields) → strings, then for/match + short-circuit and/or toward the 1.6 gate.
- **2026-08-28 (7)** — **Fixed arrays round-trip end-to-end** (the aggregate the VRA most needs). Uniform pointer model: an array `alloca` decays to an element-base pointer (`v0 = slot0;`), `elem_ptr` adds the index (`&v0[i]`); `build.h ir_alloca_array`, `lower.h` STMT_VAR aggregate path (array-literal init stores each element) + `IrLocal.aggregate`, `emit_c.h` alloca-type map declares `elem slot[N]` and emits array vs scalar allocas. Three tests round-trip **lower→emit C→gcc→run, all exit 0**: literal init + read (`array1`), index write (`array2`), and the canonical VRA pattern **loop-fill `while i<4 { a[i]=i; i=i+1 }`** (`array3` — scalar `i` reloaded at the loop header, `i+1` ℤ-widened then truncated on store: the memory-form the octagon analyzer will consume). Old engine green (**614**, +3 new tests pass it too). Next: slices (fat pointers) → structs → strings, then for/match + short-circuit and/or toward the 1.6 gate.
- **2026-08-28 (6)** — **END-TO-END: a real program round-trips the whole NEW middle-end+backend and RUNS CORRECTLY.** `src/ir/emit_c.h` (IR→C, scalar core) + `lower_driver.c --emit-c`: `tests/ir/sum_maxi.ln` → lower → emit C → gcc → exit 0 (sum(5)==10). Scalar core of Phase 1 (1.1-1.3) done. Next: grow lowering+backend coverage (arrays/slices/structs/strings) toward the 1.6 gate. Old engine green (611).
- **2026-08-28 (5)** — **Phase 1.2 lowering WORKS** end-to-end: `src/ir/lower.h` + `lower_driver.c` lower a real parsed+typechecked program (`tests/ir/sum_maxi.ln`) to a correct CFG (loop header detected; ℤ-widened adds). Core subset done; grow coverage next (for/match/structs/strings). Old engine green (611).
- **2026-08-28 (4)** — Phase 1: IR construction API + dumper DONE + validated (`src/ir/build.h`, `dump.h`, `test_ir.c` — hand-built branch + loop dump correctly, loop-header auto-detected). 1.1 complete, 1.5 dumper done. Next: **1.2 AST→IR lowering** (`src/ir/lower.h`) wired to the frontend (parse+typecheck→typed AST→lower→dump), then 1.3 IR→C. Old engine green (610).
- **2026-08-28 (3)** — Phase 0 complete; **Phase 1 STARTED**: `src/ir/ir.h` first draft (IR data structures) landed + compiles standalone. Next: 1.2 AST→IR lowering + builder defs + dumper.
- **2026-08-28 (2)** — **PHASE 0 COMPLETE.** All design docs done: 0.1 architecture, 0.2 ir,
  0.3 vra-octagon, 0.4 recognizer-catalog (validates the thesis: 70 recognizers → ~50 fall
  out / ~9 transfer / 1 lemma), 0.5 spec ch.13 contract (abstract, decoupled), 0.6 domain
  staging locked (closed-DBM first → octagons). **Next: PHASE 1 — the IR foundation.** Start
  1.1 `src/ir/ir.h` (IR data structures per `design/ir.md`), then 1.2 AST→IR lowering, 1.3
  IR→C, 1.4 `--engine=ir` selector, 1.6 GATE: every `*_pass` round-trips through the new
  pipeline. Old engine unchanged and green (suite 610, trust 40, fuzzers clean).
- **2026-08-28 (1)** — Plan + three core design docs (0.1–0.3) done.
