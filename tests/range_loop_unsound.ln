func require_one(x int) int
    pre x == 1
{
    return 0
}

func main() int {
    var x = 0
    for i in 0..10 {
        x = x + 1
    }
    // x is actually 10.
    // Compiler thinks x is 1 (0 + 1).
    // So this call (pre x == 1) should PASS if unsound.
    return require_one(x)
}
