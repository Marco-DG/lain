// EXPECT: [E085]
// SOUNDNESS: `if o + 1 < k` bounds o+1, not o+2 — `out[o+2]` can reach k. Rejected.
proc f(var out i32[k], k usize, o0 usize) {
    var o usize = o0
    if o + 1 < k {
        out[o + 2] = 1
    }
}
proc main() i32 { return 0 }
