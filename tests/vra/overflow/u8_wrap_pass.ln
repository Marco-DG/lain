// Using the wrapping operator +% on u8 is explicitly safe (modular).
// The result is intentional wraparound, not undefined overflow.

proc main() i32 {
    var a u8 = 200
    var b u8 = 100
    var c u8 = a +% b   // 300 mod 256 = 44, explicit wrap
    return 0
}
