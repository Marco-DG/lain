// EXPECT: [E086]
// u8 + u8 may produce a value > 255: assignment to u8 must fail.

proc main() i32 {
    var a u8 = 200
    var b u8 = 100
    var c u8 = a + b   // range [200+100..] cannot fit u8
    return 0
}
