// In-bounds sub-slicing of a slice must still be accepted and produce correct
// values (regression guard for the size_expr bounds extension).
proc main() i32 {
    var a i32[4] = [1, 2, 3, 4]
    var s = a[0..4]
    var t = s[2..4]
    return t[0] + t[1]
}
