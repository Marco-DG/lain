// 02_borrow_basic.ln
// Tests borrowing rules.

type Point {
    x int
    y int
}

func read(p Point) int {
    return p.x + p.y
}

func update(p var Point) {
    p.x = p.x + 1
}

func main() int {
    var p Point
    p.x = 10
    p.y = 20
    
    // Shared borrow
    r1 = read(p)
    r2 = read(p)
    
    // Mutable borrow
    update(var p)
    
    return p.x // Should be 11
}
