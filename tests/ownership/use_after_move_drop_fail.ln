// EXPECT: [E001]
// Lock: use of a linear variable after it is moved is rejected.
type R {
    h i32
}
func drop(mov {h} R) { }
func sink(mov r R) { drop(mov r) }
func main() i32 {
    var a = R(1)
    sink(mov a)
    return a.h
}
