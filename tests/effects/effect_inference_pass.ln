// F3.3 effect row (E1): direct-effect inference. Each function's effect set is
// computed (--dump-effects surfaces it): pure_add {} pure&total, with_panic
// {Raises}, mutates {Write}, does_io {IO}, diverges {Diverge}. This test locks
// that the effect pass does not break compilation of the effect-bearing forms.
// D-40: an extern takes a pointer and a length, not a slice — a slice has no C type, so the
// declaration could not describe the callee (E131). std/ already declares its externs this way.
extern proc print(s *u8, n usize)
func pure_add(a i32, b i32) i64 {
    return a + b
}
func with_panic(a i32) i32 {
    if a < 0 {
        panic("neg")
    }
    return a
}
func mutates(var c i32) {
    c = c +% 1
}
func does_io() effects io {
    msg = "hi"
    print(msg.data, msg.len)
}
func diverges(n i32) i32 effects diverge {
    var i = 0
    while i < n {
        i = i +% 1
    }
    return i
}
func main() i32 { return 0 }
