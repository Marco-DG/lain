// Sprint 11 — Q-014: panic builtin emits abort + stderr message

func check(x i32) i32 effects raises {
    if x < 0 {
        panic("negative value")
    }
    return x
}

func main() i32 effects raises {
    return check(42)
}
