// Test: safe division with new equation-style constraint
// OLD SYNTAX: pre b != 0 (REMOVED)
// NEW SYNTAX: b int != 0

func safe_div(a int, b int != 0) int {
    return a / b
}

func main() int {
    var x = 10
    var y = 2
    var z = safe_div(x, y)
    return 0
}
