// Implicit immutable declaration: bare `name = expr` creates an
// immutable binding when `name` is not in scope.
// Reassigning it is E009.
// EXPECT: [E009]

proc main() i32 {
    x = 1
    x = 2
    return x
}
