// EXPECT: [E019]
// Using a nested struct as a whole with an unset leaf is caught.
type Inner {
    x i32
    y i32
}
type Outer {
    a Inner
    b i32
}
func rd(p Outer) i32 { return p.b }
proc main() i32 {
    var p Outer
    p.a.x = 1
    p.b = 2
    return rd(p)
}
