extern proc libc_printf(fmt *u8, ...) i32
func get(a i32[], i usize < a.len) i32 {
    return a[i]
}
func main() i32 effects io {
    var arr = [10, 20, 30]
    libc_printf("%d\n", get(arr, 2))
    return 0
}
