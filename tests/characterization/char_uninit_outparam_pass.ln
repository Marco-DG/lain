// Passing `var v` to a mutable parameter is the out-param init pattern: the
// callee may initialize v, so it must NOT be flagged as an uninitialized read.
proc setit(x var i32) {
    x = 9
}
proc main() i32 {
    var v i32
    setit(var v)
    return v
}
