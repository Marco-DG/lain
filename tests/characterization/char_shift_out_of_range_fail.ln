// EXPECT: [E086]
// Shifting by a constant >= the operand's bit width is UB (gcc: "shift count
// >= width of type"). Was unchecked: `a << 40` on an i32 compiled.
func f(a i32) i32 {
    return a << 40
}
proc main() i32 {
    return 0
}
