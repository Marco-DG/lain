func Option(comptime T type) type {
    return type {
        Some { value T }
        None
    }
}

type OptionInt = Option(i32)

proc main() {
    var a OptionInt = OptionInt.Some(42)
    var b OptionInt = OptionInt.None
}
