// Union / optional types as struct fields — one construct. A self-referential
// linked-list node is a bare pointer (none = null); an optional field is `T | none`.
type Node {
    val  i32
    next *Node | none
}
type Box {
    ptr *u8 | none
    tag i32
}
proc main() i32 { return 0 }
