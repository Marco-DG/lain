// Test: return constraint violation - should FAIL to compile
// The function claims to return int >= 0 but returns -1

func bad_abs(x int) int >= 0 {
    return -1 // Violation!
}

func main() int {
    var a = bad_abs(10)
    return 0
}
