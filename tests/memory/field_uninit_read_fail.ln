// EXPECT: [E019]
// ADJUDICATED 2026-09-08: [E019] rather than [E005], same reason as
// array_uninit_element_read_fail — the struct is partially initialised, not untouched.
// Reading a struct field that was never written is caught (was silent garbage).
type Point {
    x i32
    y i32
}
proc main() i32 {
    var p Point
    p.x = 5
    return p.y
}
