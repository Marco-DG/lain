// EXPECT: [E083]
// Sprint 6 — Q-008: omitting `mov` on a linear field is a compile error.

type Handle {
    mov ptr *i32
}

type Wrapper {
    h Handle           // ❌ missing `mov` — Handle is linear
}

proc main() i32 {
    return 0
}
