// EXPECT: [E086]
// u8 - u8 may produce a negative value: cannot fit u8.

proc main() i32 {
    var a u8 = 10
    var b u8 = 200
    var c u8 = a - b   // range underflow into negatives
    return 0
}
