// Test: return type constraints - should FAIL to compile
// The function claims to return i32 >= 0 but returns -1

func bad_abs(x i32) i32 >= 0 {
    return -1  // ERROR: -1 does not satisfy >= 0
}

proc main() i32 {
    var a = bad_abs(5)
    return 0
}
