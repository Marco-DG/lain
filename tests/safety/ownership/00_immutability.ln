// 00_immutability.ln
// Tests that default bindings are immutable.

proc main() int {
    var x = 10
    // x = 20      // would be fine now that x is var — this test focuses on y's mutability
    
    var y = 10
    y = 20         // OK
    
    return y
}
