// 00_immutability.ln
// Tests that default bindings are immutable.

proc main() int {
    x = 10
    // x = 20      // ERROR: Cannot assign to immutable variable
    
    var y = 10
    y = 20         // OK
    
    return y
}
