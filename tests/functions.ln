extern func printf(fmt u8[:0], ...) int

func add(a int, b int) int {
    return a + b
}

func fib(n int) int {
    if n < 2 { return n }
    return fib(n-1) + fib(n-2)
}

func main() int {
    printf("add(10, 20) = %d\n", add(10, 20))
    printf("fib(10) = %d\n", fib(10))
    return 0
}
