// Test for match exhaustiveness: should FAIL compilation
// Match without else case on integer (not exhaustive)

func main() int {
    var x = 5
    
    match x {
        1: return 1
        2: return 2
        3: return 3
        // No else case! Not exhaustive for integers
    }
    
    return 0
}
