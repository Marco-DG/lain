// Test for case exhaustiveness: should FAIL compilation
// Case without else clause on integer (not exhaustive)

func main() int {
    var x = 5
    
    case x {
        1: return 1
        2: return 2
        3: return 3
        // No else case! Not exhaustive for integers
    }
    
    return 0
}
