// EXPECT: [E011]
// A `func` without a `decreasing` clause still forbids recursion (must be total).
func f(n usize) usize {
    if n < 1 { return 0 }
    return f(n - 1)
}
proc main() i32 { return f(3) as i32 }
