// EXPECT: [E015]
// Test: division by zero in pure function should fail
// Expected: Compiler error — potential division by zero, divisor range includes zero
func unsafe_div(a i32, b i32) i32 {
    return a / b
}

proc main() {
    unsafe_div(10, 5)
}
