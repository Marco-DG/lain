// EXPECT: [E082]
// Test: bounded while with wrong direction should be rejected
// Expected: compilation should FAIL (measure does not decrease)

func bad(n i32) i32 {
    var i = 0
    while i < n decreasing n - i {
        i -= 1
    }
    return i
}

proc main() i32 {
    return bad(10)
}
