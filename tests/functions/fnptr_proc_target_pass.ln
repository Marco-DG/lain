// A *proc target accepts a total func (totality subtyping: func <: proc).
func idn(a i32) i32 { return a }
func main() i32 effects diverge, io, raises {
    var g *proc(i32) i32 = idn
    if g(5) == 5 { return 0 }
    return 1
}
