// F-003 regression: 'fun' must no longer be silently aliased to 'func'.
// Prior to fix: compiled as a func definition.
// After fix: parser rejects (E100).
// EXPECT: [E100]

fun add(a i32, b i32) i32 {
    return a + b
}

proc main() i32 {
    return add(1, 2)
}
