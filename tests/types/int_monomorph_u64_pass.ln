// Sprint 1 Phase 3: int monomorphization should also fire for u64
// arguments, not only i64. Verifies symmetric handling.

func passthrough(n int) int {
    return n
}

proc main() int {
    var big u64 = 18000000000
    var d = passthrough(big)    // instance passthrough_int_u64
    return 0
}
