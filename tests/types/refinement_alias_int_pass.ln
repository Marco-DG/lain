// Q-002 refinement alias: types based on `int` with multiple comparison
// operators (>=, <=, !=) chain via `and`.

type SmallPos = int >= 1 and <= 10
type NonZero = int != 0

proc main() int {
    var p SmallPos = 5
    var n NonZero = 42
    return 0
}
