// Q-002 refinement alias: types based on `i32` with multiple comparison
// operators (>=, <=, !=) chain via `and`.

type SmallPos = i32 >= 1 and <= 10
type NonZero = i32 != 0

proc main() i32 {
    var p SmallPos = 5
    var n NonZero = 42
    return 0
}
