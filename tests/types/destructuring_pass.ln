extern func libc_printf(fmt *u8, ...) i32 effects io

type Point {
    x u8
    y u8
}

func main() i32 effects io {
    var p = Point(10, 20)
    
    // Test manual destructuring / access
    var x = p.x
    var y = p.y
    libc_printf("Point: x=%d, y=%d\n", x, y)
    
    return 0
}
