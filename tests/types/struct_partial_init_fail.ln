// EXPECT: [E012]
// Test: Partially initialized struct via constructor should fail

type Point {
    x i32
    y i32
    z i32
}

proc main() i32 {
    var p = Point(10, 20) // Missing z
    return 0
}
