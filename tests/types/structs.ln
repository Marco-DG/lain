extern proc libc_printf(fmt *u8, ...) i32

type Vec2 {
    x i32
    y i32
}

proc main() i32 {
    var v Vec2 = undefined
    v.x = 10
    v.y = 20
    libc_printf("v = {%d, %d}\n", v.x, v.y)
    
    var v2 = v
    v2.x = 30
    libc_printf("v2.x = %d, v.x = %d\n", v2.x, v.x)
    return 0
}
