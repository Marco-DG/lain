// Test: array literal syntax [1, 2, 3]

extern func printf(fmt *u8, ...) i32 effects io

func main() i32 effects io {
    var arr = [10, 20, 30, 40, 50]
    printf("arr[0]=%d, arr[2]=%d, arr[4]=%d\n", arr[0], arr[2], arr[4])
    return 0
}
